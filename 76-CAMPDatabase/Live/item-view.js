    const CategoryIcons = {
      'Quest':'../assets/MainCategory-QuestsIcon.svg','Objectives':'../assets/MainCategory-QuestsIcon.svg','Modify':'../assets/MainCategory-ModifyIcon.svg','Wallpapers':'../assets/MainCategory-WallpapersIcon.svg','C.A.M.P. Pieces':'../assets/MainCategory-CAMPPiecesIcon.svg','Foundations':'../assets/SubCategory-FoundationsIcon.webp','Decorations':'../assets/MainCategory-DecorationsIcon.svg','Defense':'../assets/MainCategory-DefensesIcon.svg','Dwellers':'../assets/MainCategory-DwellersIcon.svg','Furniture':'../assets/MainCategory-FurnitureIcon.svg','Lights':'../assets/MainCategory-LightsIcon.svg','Power':'../assets/MainCategory-PowerIcon.svg','Resources':'../assets/MainCategory-ResourcesIcon.svg','Storage':'../assets/MainCategory-StorageIcon.svg','Utility':'../assets/MainCategory-UtilityIcon.svg','Wall Decor':'../assets/MainCategory-WallDecorIcon.svg','Structure':'../assets/MainCategory-StructureIcon.svg','Porches':'../assets/SubCategory-PorchesIcon.webp','Floors':'../assets/SubCategory-FloorsIcon.webp','Walls':'../assets/SubCategory-WallsIcon.webp','Roofs':'../assets/SubCategory-RoofsIcon.webp','Stairs':'../assets/SubCategory-StairsIcon.webp','Shelters':'../assets/SubCategory-SheltersIcon.webp','Doors':'../assets/SubCategory-DoorsIcon.webp','Columns':'../assets/SubCategory-ColumnsIcon.webp','Fences':'../assets/SubCategory-FencesIcon.webp','Barricades':'../assets/SubCategory-BarricadeIcon.webp','Traps':'../assets/SubCategory-TrapsIcon.webp','Turrets':'../assets/SubCategory-TurretsIcon.webp','Generators':'../assets/SubCategory-GeneratorsIcon.webp','Power Connectors':'../assets/SubCategory-PowerConnectorsIcon.webp','Candles':'../assets/SubCategory-CandleIcon.webp','Ceiling Lights':'../assets/SubCategory-CeilingLightsIcon.webp','Fire':'../assets/SubCategory-FireIcon.webp','Lamps':'../assets/SubCategory-LampsIcon.svg','Wall Lights':'../assets/SubCategory-WallLightsIcon.webp','Crafting':'../assets/SubCategory-CraftingIcon.webp','Collectors':'../assets/SubCategory-CollectorsIcon.webp','Food':'../assets/SubCategory-FoodIcon.webp','Producers':'../assets/SubCategory-ProducersIcon.webp','Water':'../assets/SubCategory-WaterIcon.webp','Instruments':'../assets/SubCategory-InstrumentsIcon.webp','Player Buffs':'../assets/SubCategory-PlayerBuffsIcon.webp','Services':'../assets/SubCategory-ServicesIcon.webp','Vending Machines':'../assets/SubCategory-VendingMachinesIcon.webp','Appliances':'../assets/SubCategory-AppliancesIcon.webp','Beds':'../assets/SubCategory-BedsIcon.webp','Electronics':'../assets/SubCategory-ElectronicsIcon.webp','Seating':'../assets/SubCategory-SeatingIcon.webp','Shelves':'../assets/SubCategory-ShelvesIcon.webp','Surfaces':'../assets/SubCategory-SurfacesIcon.webp','Balloons':'../assets/SubCategory-BalloonsIcon.webp','Clutter':'../assets/SubCategory-ClutterIcon.webp','Crockery':'../assets/SubCategory-CrockeryIcon.webp','Entertainment':'../assets/SubCategory-EntertainmentIcon.webp','Fauna':'../assets/SubCategory-FaunaIcon.webp','Lawn & Garden':'../assets/SubCategory-Lawn&GardenIcon.webp','Novelties':'../assets/SubCategory-NoveltiesIcon.webp','Outdoor':'../assets/SubCategory-OutdoorIcon.webp','Rugs':'../assets/SubCategory-RugsIcon.webp','Signs':'../assets/SubCategory-SignsIcon.webp','Statues':'../assets/SubCategory-StatuesIcon.webp','Taxidermy':'../assets/SubCategory-TaxidermyIcon.webp','Toys':'../assets/SubCategory-ToysIcon.webp','Vehicles':'../assets/SubCategory-VehiclesIcon.webp','Accents':'../assets/SubCategory-AccentsIcon.webp','Ceiling':'../assets/SubCategory-CeilingIcon.webp','Holiday':'../assets/SubCategory-HolidayIcon.webp','Mounted':'../assets/SubCategory-MountedIcon.webp','Tapestry':'../assets/SubCategory-TapestryIcon.webp','Wall Art':'../assets/SubCategory-WallArtIcon.webp','Wall Letters':'../assets/SubCategory-WallLettersIcon.webp','Window':'../assets/SubCategory-WindowIcon.webp','Additional Storage':'../assets/SubCategory-AdditionalStorageIcon.svg','Displays':'../assets/SubCategory-DisplaysIcon.webp','Stash Boxes':'../assets/SubCategory-StashBoxesIcon.webp','Farm':'../assets/SubCategory-FarmIcon.webp','Frontier':'../assets/SubCategory-FrontierIcon.webp','Industrial':'../assets/SubCategory-IndustrialIcon.webp','Military':'../assets/SubCategory-MilitaryIcon.webp','Modern':'../assets/SubCategory-ModernIcon.webp','Retail':'../assets/SubCategory-RetailIcon.webp','Rustic':'../assets/SubCategory-RusticIcon.webp','Scavenger':'../assets/SubCategory-ScavengerIcon.webp','Allies':'../assets/SubCategory-AlliesIcon.webp','Pets':'../assets/SubCategory-PetsIcon.webp','Pet Furniture':'../assets/SubCategory-PetFurnitureIcon.webp'
    };

    const BookTagIcons = {
      'UnusedContent':'../assets/UnusedContentIcon.svg','BullionVendor':'../assets/BullionIcon.webp','StampsVendor':'../assets/StampIcon.webp','DailyOps':'../assets/DOpsIcon.webp','CommonWorkshop':'../assets/WorkshopIcon.svg','RaidsReward':'../assets/RaidIcon.svg','TreasureHunters':'../assets/TreasureHuntersIcon.webp','MeatWeekRewards':'../assets/MeatWeekIcon.svg','FasnachtRewards':'../assets/FasnachtIcon.svg','InvadersRewards':'../assets/InvadersIcon.webp','HolidayScorched':'../assets/HolidayScorchedIcon.webp','BigBloomRewards':'../assets/BigBloomIcon.webp','MischiefNightRewards':'../assets/MischiefNightIcon.svg','SpookyScorched':'../assets/SpookyScorchedIcon.svg','PublicEventRewards':'../assets/PublicEventIcon.svg','EquinoxRewards':'../assets/EquinoxIcon.svg','GrahmVendor':'../assets/GrahmIcon.webp','ColossalProblemRewards':'../assets/PublicEventIcon.svg','ScorchedEarthRewards':'../assets/PublicEventIcon.svg','BeastsOfBurdenRewards':'../assets/PublicEventIcon.svg','CampfireTalesRewards':'../assets/PublicEventIcon.svg','DangerousPastimesRewards':'../assets/PublicEventIcon.svg','MoonshineJamboreeRewards':'../assets/PublicEventIcon.svg','MostWantedRewards':'../assets/PublicEventIcon.svg','NWOTGenericRewards':'../assets/PublicEventIcon.svg','NeurologicalWarfareRewards':'../assets/PublicEventIcon.svg','RadiationRumbleRewards':'../assets/PublicEventIcon.svg','SafeAndSoundRewards':'../assets/PublicEventIcon.svg','SeismicActivityRewards':'../assets/PublicEventIcon.svg','SpinTheWheelRewards':'../assets/PublicEventIcon.svg','TestYourMetalRewards':'../assets/PublicEventIcon.svg','TunnelOfLoveRewards':'../assets/PublicEventIcon.svg','GearinUpRewards':'../assets/PublicEventIcon.svg','SinkholeSolutionsRewards':'../assets/PublicEventIcon.svg'
    };

    const multiVendorCategories = {
      BullionVendor:{icon:'../assets/BullionIcon.webp',prefixes:['BullionVendorSamuel','BullionVendorMinerva','BullionVendorMortimer','BullionVendorRegs','BullionVendorWindy']},
      RobotVendor:{icon:'../assets/RobotVendorIcon.webp',prefixes:['FreeStatesVendor','BrotherhoodVendor','RespondersVendor','RaiderVendor','WhitespringStationVendor','PendletonVendor']},
      NPCVendor:{icon:'../assets/NPCVendorIcon.webp',prefixes:['MacVendor','JuneSeaverVendor']}
    };

    const ManualEntmIcons = {'Babylon_':'../assets/V51Icon.webp','Shelters_':'../assets/AtomIcon.webp','DE2024_Halloween':'../assets/ChallengeIcon.svg','DE2024_ScoutsUncharted':'../assets/ChallengeIcon.svg','DE2024_ScienceOfLove':'../assets/ChallengeIcon.svg'};
    const miniSeasonAliases = {'SCORE_MiniSeason_2025_MMMFE':'Marshal Mallow','SCORE_MiniSeason_Lovehurts':'Love Hurts'};
    const IconAltText_ENTM = {"F1_ENTM_CAMP_SCRAPBOX_STANDARD":"Requires Fallout 1st Subscription","F1_ENTM_CAMP_AMMOSTORAGEBOX_STANDARD":"Requires Fallout 1st Subscription","F1_ENTM_CAMP_AIDBOX_STANDARD":"Requires Fallout 1st Subscription",BABYLON_:"Nuclear Winter Reward",SHELTERS_:"Atomic Shop Item",DE2024_Halloween:"Trick or Treat Challenges Event Reward (2024)",DE2024_ScienceOfLove:"Science of Love Challenges Event Reward (2024)",DE2024_ScoutsUncharted:"Scouts Uncharted Challenges Event Reward (2024)",ATX:"Atomic Shop Item",SCORE:"S.C.O.R.E. Reward"};
    const cnamUnlockText = {'00424740':'(Unlock 2 or more)','0042716B':'(Unlock 3 or more)','0042716D':'(Unlock 3 or more)','0042716C':'(Unlock 4 or more)'};
    const MUTUALLY_EXCLUSIVE_BUFFS = new Set(['Well Rested', "Lover's Embrace", 'Kindred Spirit']);
    const IconAltText_BOOK = {
      BullionVendor: {
        vendors: ['Minerva', 'Mortimer', 'Samuel', 'Regs', 'Windy'],
        get alt() { return `Sold by Gold Bullion Vendor (${this.vendors.join(', ')})`; }
      },
      StampsVendor: { alt: "Sold by Giuseppe Della Ripa" },
      DailyOps: { alt: "Daily Ops Reward" },
      CommonWorkshop: { alt: "Common Workshop Claim/Defense Reward" },
      RaidsReward: { alt: "Gleaming Depths Raid Reward" },
      TreasureHunters: { alt: "Treasure Hunters Event Reward" },
      MeatWeekRewards: { alt: "Meat Week Seasonal Event Reward" },
      FasnachtRewards: { alt: "Fasnacht Seasonal Event Reward" },
      InvadersRewards: { alt: "Invaders from Beyond Seasonal Event Reward" },
      HolidayScorched: { alt: "Holiday Scorched Seasonal Event Reward" },
      BigBloomRewards: { alt: "Big Bloom Seasonal Event Reward" },
      MischiefNightRewards: { alt: "Mischief Night Seasonal Event Reward" },
      SpookyScorched: { alt: "Spooky Scorched Seasonal Event Reward" },
      PublicEventRewards: { alt: "Public Event Reward" },
      EquinoxRewards: { alt: "Mothman Equinox Seasonal Event Reward" },
      GrahmVendor: { alt: "Sold by Grahm" },
      UnusedContent: { alt: "Unused Content" },
      ScorchedEarthRewards: { alt: "Reward from Event: Scorched Earth" },
      ColossalProblemRewards: { alt: "Reward from Event: A Colossal Problem" },
      BeastsOfBurdenRewards: { alt: "Reward from Event: Beasts of Burden" },
      CampfireTalesRewards: { alt: "Reward from Event: Campfire Tales" },
      DangerousPastimesRewards: { alt: "Reward from Event: Dangerous Pastimes" },
      MoonshineJamboreeRewards: { alt: "Reward from Event: Moonshine Jamboree" },
      MostWantedRewards: { alt: "Reward from Event: Most Wanted" },
      NWOTGenericRewards: { alt: "Reward from Event: Most Wanted, Event: Spin the Wheel, Event: Tunnel of Love" },
      NeurologicalWarfareRewards: { alt: "Reward from Event: Neurological Warfare" },
      RadiationRumbleRewards: { alt: "Reward from Event: Radiation Rumble" },
      SafeAndSoundRewards: { alt: "Reward from Event: Safe and Sound" },
      SeismicActivityRewards: { alt: "Reward from Event: Seismic Activity" },
      SpinTheWheelRewards: { alt: "Reward from Event: Spin the Wheel" },
      TestYourMetalRewards: { alt: "Reward from Event: Test Your Metal" },
      TunnelOfLoveRewards: { alt: "Reward from Event: Tunnel of Love" },
      GearinUpRewards: { alt: "Reward from Event: Gearin\' Up" },
      SinkholeSolutionsRewards: { alt: "Reward from Event: Sinkhole Solutions" },
    };

    // ── Copy-to-clipboard utility ──
    function copyToClipboard(el, text) {
      navigator.clipboard.writeText(text).then(() => {
        el.classList.add('copied');
        setTimeout(() => { el.classList.remove('copied'); }, 1000);
      }).catch(() => {});
    }

    // ── Detail tooltip (Model paths / Material Swaps "?" popovers) ──
    //
    // Unlike a plain hover tooltip, this one:
    //  - stays open while the mouse is over the popup itself, so long
    //    paths can be selected/copied instead of vanishing the moment the
    //    cursor leaves the "?" icon
    //  - accepts rich DOM content (contentBuilder), not just plain text,
    //    so e.g. material swap pairs can use a real arrow graphic instead
    //    of "->"
    //  - positions itself off the whole containing .infobox-row rather
    //    than just the icon, so it can't overlap sibling text that
    //    happens to have wrapped onto a line below the icon
    //  - on touch, stays open until the user taps elsewhere (rather than
    //    auto-hiding after a couple seconds), since the point is usually
    //    to read/copy something
    // Tooltip boxes are appended directly to document.body (so they can float
    // above everything, positioned off the icon rather than clipped by any
    // ancestor). Because of that they live outside contentDiv and don't get
    // cleared by contentDiv.replaceChildren() on navigation — track them here
    // so loadItemRoute can remove the previous item's tooltips before
    // rendering the next one.
    let _activeDetailTooltips = [];
    function clearDetailTooltips() {
      _activeDetailTooltips.forEach(box => box.remove());
      _activeDetailTooltips = [];
    }

    function attachDetailTooltip(icon, contentBuilder, opts) {
      const sticky = opts && opts.sticky;
      const isTouch = 'ontouchstart' in window || navigator.maxTouchPoints > 0;

      const box = document.createElement('div');
      box.className = 'detail-tooltip';
      if (sticky) box.classList.add('detail-tooltip--sticky');
      contentBuilder(box);
      document.body.appendChild(box);
      _activeDetailTooltips.push(box);

      let locked = false;

      function position() {
        const iconRect = icon.getBoundingClientRect();
        const boxRect = box.getBoundingClientRect();

        let left = iconRect.left + iconRect.width / 2 - boxRect.width / 2;
        left = Math.max(8, Math.min(left, window.innerWidth - boxRect.width - 8));

        let top = iconRect.top - boxRect.height - 10;
        if (top < 8) top = iconRect.bottom + 10; // flip below if no room above

        box.style.left = `${left}px`;
        box.style.top = `${top}px`;
      }

      function show() { position(); box.classList.add('show'); }
      function hide() { locked = false; box.classList.remove('show'); }
      box._detailHide = hide;

      if (!isTouch) {
        let hideTimer = null;
        const scheduleHide = () => { hideTimer = setTimeout(hide, 200); };
        const cancelHide = () => clearTimeout(hideTimer);

        icon.addEventListener('mouseenter', () => { cancelHide(); show(); });
        icon.addEventListener('mouseleave', () => { if (!locked) scheduleHide(); });
        box.addEventListener('mouseenter', cancelHide);
        box.addEventListener('mouseleave', () => { if (!locked) scheduleHide(); });

        if (sticky) {
          icon.addEventListener('click', (e) => {
            e.stopPropagation();
            if (locked) { hide(); return; }
            locked = true;
            cancelHide();
            show();
          });
          box.addEventListener('click', (e) => { e.stopPropagation(); });
        }
      } else {
        icon.addEventListener('click', (e) => {
          e.stopPropagation();
          document.querySelectorAll('.detail-tooltip.show').forEach(el => el.classList.remove('show'));

          let mobileBox = document.getElementById('detail-mobile-tooltip');
          if (!mobileBox) {
            mobileBox = document.createElement('div');
            mobileBox.id = 'detail-mobile-tooltip';
            document.body.appendChild(mobileBox);
          }
          mobileBox.innerHTML = '';
          mobileBox.classList.remove('detail-tooltip--swap');
          Array.from(box.children).forEach(child => {
            mobileBox.appendChild(child.cloneNode(true));
          });
          if (box.classList.contains('detail-tooltip--swap')) {
            mobileBox.classList.add('detail-tooltip--swap');
          }
          void mobileBox.offsetWidth;
          mobileBox.classList.add('show');
          clearTimeout(mobileBox._hideTimer);
          mobileBox._hideTimer = setTimeout(() => mobileBox.classList.remove('show'), 5000);
        });
      }
    }

    // Tap-outside-to-close for the touch variant above.
    document.addEventListener('click', (e) => {
      if (e.target.closest('.detail-tooltip') || e.target.closest('.info-icon') || e.target.closest('#detail-mobile-tooltip')) return;
      document.querySelectorAll('.detail-tooltip.show').forEach(el => {
        if (el._detailHide) el._detailHide(); else el.classList.remove('show');
      });
      const mb = document.getElementById('detail-mobile-tooltip');
      if (mb) mb.classList.remove('show');
    });

    function buildPathTooltipContent(box, path) {
      const pill = document.createElement('div');
      pill.className = 'path-pill';
      pill.textContent = path;
      pill.addEventListener('click', (e) => {
        e.stopPropagation();
        copyToClipboard(pill, path);
      });
      box.appendChild(pill);
    }

    function buildMaterialSwapTooltipContent(box, pairs) {
      box.classList.add('detail-tooltip--swap');

      const header = document.createElement('div');
      header.className = 'swap-header';
      const srcLbl = document.createElement('span');
      srcLbl.className = 'swap-header-label';
      srcLbl.textContent = 'Original Material';
      const spacer = document.createElement('span');
      const tgtLbl = document.createElement('span');
      tgtLbl.className = 'swap-header-label';
      tgtLbl.textContent = 'Swap Material';
      header.appendChild(srcLbl);
      header.appendChild(spacer);
      header.appendChild(tgtLbl);
      box.appendChild(header);

      const REMAP_RE = /\s*\(Color(?:\s+Remap)?\s+Index[^)]*\)\s*$/i;
      const REMAP_VAL_RE = /([0-9]+(?:\.[0-9]+)?)/;

      function buildSwapPill(fullPath, cls) {
        const pill = document.createElement('div');
        pill.className = 'swap-pill ' + cls;

        const remapMatch = fullPath.match(REMAP_RE);
        const basePath = remapMatch ? fullPath.slice(0, remapMatch.index) : fullPath;
        const remapRaw = remapMatch ? remapMatch[0].trim() : null;

        const pathText = document.createElement('div');
        pathText.className = 'path-text';
        pathText.textContent = basePath;
        pill.appendChild(pathText);

        if (remapRaw) {
          const line = document.createElement('div');
          line.className = 'remap-line';

          const lbl = document.createElement('span');
          lbl.className = 'remap-label';
          lbl.textContent = 'Color Remap:';
          line.appendChild(lbl);

          const valMatch = remapRaw.match(REMAP_VAL_RE);
          const valStr = valMatch ? valMatch[0] : remapRaw;
          const valSpan = document.createElement('span');
          valSpan.className = 'remap-val';
          valSpan.textContent = valStr;
          line.appendChild(valSpan);

          line.addEventListener('click', (e) => {
            e.stopPropagation();
            copyToClipboard(line, valStr);
          });
          pill.appendChild(line);
        }

        pill.addEventListener('click', (e) => {
          e.stopPropagation();
          copyToClipboard(pill, basePath);
        });
        return pill;
      }

      pairs.forEach(pair => {
        const arrowIdx = pair.indexOf('->');
        if (arrowIdx === -1) return;

        const sourcePath = pair.slice(0, arrowIdx).trim();
        const targetPath = pair.slice(arrowIdx + 2).trim();

        const row = document.createElement('div');
        row.className = 'swap-mapping-row';

        row.appendChild(buildSwapPill(sourcePath, 'swap-pill--source'));

        const arrow = document.createElement('span');
        arrow.className = 'swap-arrow';
        arrow.textContent = '\u25B6';
        row.appendChild(arrow);

        row.appendChild(buildSwapPill(targetPath, 'swap-pill--target'));

        box.appendChild(row);
      });
    }

    function getWeatherType(weatherKeywords) {
      if (!weatherKeywords) return 'Clear';
      const keywords = String(weatherKeywords).split(';').map(k => k.trim()).filter(Boolean);
      const radstormKeywords = ['s_wt_StormRad', 's_wt_StormNuke'];
      const rainyKeywords = ['s_wt_StormMistyRainy', 's_wt_StormRain', 's_wt_StormRainOcclusion', 'ATX_Weather_WeatherTypeKW_ThunderStorm'];
      
      if (keywords.some(k => radstormKeywords.includes(k))) {
        return 'Radstorm / Nuked';
      }
      if (keywords.some(k => rainyKeywords.includes(k))) {
        return 'Rainy';
      }
      return 'Clear';
    }

    function normalizeTag(tag) {
      for (const [key, data] of Object.entries(multiVendorCategories)) {
        if (key === tag || (data.prefixes && data.prefixes.some(p => tag.startsWith(p)))) return key;
      }
      return tag;
    }

    function getBookIconSrc(nt, bookEditorId, bookSource) {
      if (/Challenge_/.test(bookEditorId)) return '../assets/ChallengeIcon.svg';
      if (String(bookSource||'').toLowerCase() === 'learnbypickup') return '../assets/LearnOnPickupIcon.webp';
      if (multiVendorCategories[nt]?.icon) return multiVendorCategories[nt].icon;
      return BookTagIcons[nt] || '';
    }

    function getBookTooltip(nt, origTags, bookEditorId, bookSource) {
      if (/Challenge_/.test(bookEditorId)) return 'Complete a Challenge to Unlock';
      if (String(bookSource||'').toLowerCase() === 'learnbypickup') return 'Loot to Unlock';
      if (nt === 'None') return '';
      // Normalize BullionVendor variants
      const key = nt.startsWith('BullionVendor') ? 'BullionVendor' : nt;
      if (key in multiVendorCategories) {
        const cat = multiVendorCategories[key];
        const vendors = origTags.filter(t => t===key||(cat.prefixes&&cat.prefixes.some(p=>t.startsWith(p)))).map(t=>{
          if(t===key) return key==='BullionVendor'?'Gold Bullion':key.replace('Vendor','');
          const m=cat.prefixes.find(p=>t.startsWith(p));
          return m?m.replace(key,'').replace('Vendor','').replace(/([a-z])([A-Z])/g,'$1 $2').trim():t;
        });
        if(key==='NPCVendor') return `Sold by NPC Vendor (${vendors.join(', ')})`;
        const label=key==='BullionVendor'?'Gold Bullion':key.replace('Vendor','');
        return `Sold by ${label} Vendor (${vendors.join(', ')})`;
      }
      return IconAltText_BOOK[key]?.alt || (key === 'None' ? '' : key);
    }

    // iconSrcs: string | string[]
    // flavorLines: string | string[]  — each rendered as its own line/bullet
    function makePill(text, iconSrcs, flavorLines) {
      const pill = document.createElement('span');
      pill.className = 'detail-pill';

      const icons   = iconSrcs   ? (Array.isArray(iconSrcs)   ? iconSrcs   : [iconSrcs])   : [];
      const flavors = flavorLines ? (Array.isArray(flavorLines) ? flavorLines : [flavorLines]) : [];

      // Main row: all icons + label text
      const main = document.createElement('span');
      main.className = 'detail-pill-main';
      icons.filter(Boolean).forEach(src => {
        const img = document.createElement('img');
        img.src = src;
        main.appendChild(img);
      });
      main.appendChild(document.createTextNode(text));
      pill.appendChild(main);

      // Flavor lines — deduplicated, skip if same as pill text
      const uniqueFlavors = [...new Set(flavors.filter(f => f && f !== text))];
      if (uniqueFlavors.length === 1) {
        const flavor = document.createElement('span');
        flavor.className = 'detail-pill-flavor';
        flavor.textContent = uniqueFlavors[0];
        pill.appendChild(flavor);
      } else {
        uniqueFlavors.forEach(line => {
          const flavor = document.createElement('span');
          flavor.className = 'detail-pill-flavor';
          flavor.textContent = '• ' + line;
          pill.appendChild(flavor);
        });
      }

      return pill;
    }
    function makeConnector(text) {
      const s = document.createElement('span'); s.className='detail-connector'; s.textContent=text; return s;
    }
    // Appends a set of pills to a container, each as its own individual box,
    // with a plain connector (AND/OR) between them.
    function appendPillGroup(container, pills, connector) {
      pills.forEach((pill, i) => {
        container.appendChild(pill);
        if (i < pills.length - 1) container.appendChild(makeConnector(connector));
      });
    }
    function makeArrow() {
      const s = document.createElement('span'); s.className='detail-arrow'; return s;
    }
    // Conditions now always render as a plain left-aligned stacked list
    // (see .detail-inline-wrap--conditions in item-view.css), so the old
    // post-layout measure-and-center behavior is no longer needed.
    function finalizeConditionWrap(wrap) {}

    function formatBuffDuration(seconds) {
      if (seconds >= 3600) {
        const h = seconds / 3600;
        return (h === Math.floor(h)) ? h + 'h' : h + 'h';
      }
      const m = seconds / 60;
      return (m === Math.floor(m)) ? m + 'm' : m + 'm';
    }

    function renderPlayerBuffs(block, buffs) {
      // Group entries by BuffSpell name
      const grouped = {};
      const spellOrder = [];
      buffs.forEach(b => {
        if (!grouped[b.BuffSpell]) {
          grouped[b.BuffSpell] = [];
          spellOrder.push(b.BuffSpell);
        }
        grouped[b.BuffSpell].push(b);
      });

      // Determine if all spells are mutually exclusive
      const allExclusive = spellOrder.every(name => MUTUALLY_EXCLUSIVE_BUFFS.has(name));

      // Activation line (use common WaitTime across all buffs)
      const waitTime = buffs[0].WaitTime;
      const activationDiv = document.createElement('div');
      activationDiv.className = 'buff-activation';
      const actLabel = document.createElement('strong');
      actLabel.textContent = 'Activation: ';
      activationDiv.appendChild(actLabel);
      const actSpan = document.createElement('span');
      actSpan.textContent = waitTime > 0
        ? 'Interact with object for ' + waitTime + 's to receive buff'
        : 'Receive buff instantly on activation';
      activationDiv.appendChild(actSpan);
      block.appendChild(activationDiv);

      // Provides label
      const providesDiv = document.createElement('div');
      providesDiv.className = 'buff-provides-label';
      providesDiv.textContent = allExclusive
        ? 'Provides one of the following bonuses:'
        : spellOrder.length === 1
          ? 'Provides the following bonus:'
          : 'Provides the following bonuses:';
      block.appendChild(providesDiv);

      // Buff options list
      const optionsUl = document.createElement('ul');
      optionsUl.className = 'buff-options-list';

      spellOrder.forEach(spellName => {
        const entries = grouped[spellName];
        const li = document.createElement('li');
        li.className = 'buff-option';

        const duration = entries[0].Duration;
        const strong = document.createElement('strong');
        strong.textContent = duration > 0 ? spellName + ' (' + formatBuffDuration(duration) + ')' : spellName;
        li.appendChild(strong);

        const detailsUl = document.createElement('ul');
        detailsUl.className = 'buff-details-list';
        entries.forEach(e => {
          const detailLi = document.createElement('li');
          let text = e.Description;
          if (e.Notes) text += ' (Requires Homebody Perk)';
          detailLi.textContent = text;
          detailsUl.appendChild(detailLi);
        });
        li.appendChild(detailsUl);
        optionsUl.appendChild(li);
      });

      block.appendChild(optionsUl);

      // Footnote (only when there's a wait time)
      if (waitTime > 0) {
        const footnote = document.createElement('div');
        footnote.className = 'buff-footnote';
        footnote.textContent = '* Activation wait time does not include entering or exiting furniture animations.';
        block.appendChild(footnote);
      }
    }

    document.getElementById('backBtn').addEventListener('click', (e) => {
      e.preventDefault();
      sessionStorage.removeItem('itemViewHistory');
      sessionStorage.removeItem('itemViewHistory_idx');

      let pageToRestore = 1;
      let scrollYToRestore = 0;
      try {
        const saved = sessionStorage.getItem('searchState');
        if (saved) {
          const state = JSON.parse(saved);
          pageToRestore = state.page || 1;
          scrollYToRestore = state.scrollY || 0;
        }
      } catch (err) {
        console.warn('Unable to read search state for back navigation:', err);
      }

      try {
        const currentState = JSON.parse(sessionStorage.getItem('searchState') || '{}');
        currentState.page = pageToRestore;
        currentState.scrollY = scrollYToRestore;
        sessionStorage.setItem('searchState', JSON.stringify(currentState));
      } catch (err) {
        console.warn('Unable to update search state for back navigation:', err);
      }

      window.location.href = './';
    });

    // ── Scroll lock utility ──
    let _scrollLockCount = 0;
    function lockScroll() {
      if (_scrollLockCount === 0) {
        const scrollbarWidth = window.innerWidth - document.documentElement.clientWidth;
        document.body.style.overflow = 'hidden';
        if (scrollbarWidth > 0) document.body.style.paddingRight = scrollbarWidth + 'px';
      }
      _scrollLockCount++;
    }
    function unlockScroll() {
      _scrollLockCount = Math.max(0, _scrollLockCount - 1);
      if (_scrollLockCount === 0) {
        document.body.style.overflow = '';
        document.body.style.paddingRight = '';
      }
    }

    // ── Modal close animation helper ──
    // Adds the `.closing` class (which triggers the fade/scale-out defined
    // in CSS) then removes the overlay once the animation has had time to
    // finish. Duration must stay in sync with the CSS transition length.
    // Pass `immediate` to skip the animation entirely (used when a page
    // navigation happens while a modal is open — the underlying content is
    // about to be replaced, so the modal should just vanish, not linger).
    const MODAL_CLOSE_ANIM_MS = 150;
    function animateCloseOverlay(overlay, immediate) {
      if (immediate) {
        if (overlay.parentNode) overlay.parentNode.removeChild(overlay);
        return;
      }
      overlay.classList.add('closing');
      setTimeout(() => {
        if (overlay.parentNode) overlay.parentNode.removeChild(overlay);
      }, MODAL_CLOSE_ANIM_MS);
    }

    // ── Global modal registry ──
    // Every modal opened via openModalShell() registers its close function
    // here so any open modal(s) can be force-closed in one shot — used when
    // the item route changes (e.g. clicking a shared/assignable item tile
    // navigates via a hash change, which doesn't reload the page, so a
    // stray modal would otherwise stay open on top of the new content).
    let openModalRegistry = [];
    let _lightboxPopStateHandled = false;
    function closeAllModals() {
      const toClose = openModalRegistry;
      openModalRegistry = [];
      toClose.forEach(close => close(true));
    }

    // ── Shared modal shell ──
    // Builds the overlay/wrap/modal/close(+back) button scaffold used by
    // every popup modal (specific-item detail, assignables "view more",
    // shared max-build-count "view more", shared build-slot "view more") so
    // they all share identical open/close animation and Escape/outside-click
    // handling. `onBack`, when provided, shows a back arrow that — only when
    // clicked — closes this modal and invokes onBack(); clicking outside or
    // the ✕ always just closes.
    //
    // Browser history integration: opening a modal pushes a history entry so
    // the back button / swipe-back gesture closes the modal (acting as ✕)
    // or triggers onBack if present.
    function openModalShell({ maxWidth, title, onBack, onClose } = {}) {
      const overlay = document.createElement('div');
      overlay.className = 'assignable-modal-overlay';

      const wrap = document.createElement('div');
      wrap.className = 'assignable-modal-wrap';
      if (maxWidth) wrap.style.maxWidth = maxWidth + 'px';
      wrap.addEventListener('click', e => e.stopPropagation());

      const header = document.createElement('div');
      header.className = 'assignable-modal-header';

      const modal = document.createElement('div');
      modal.className = 'assignable-modal';
      if (maxWidth) modal.style.maxWidth = maxWidth + 'px';

      let _closed = false;
      let _pendingAction = null; // 'back' when back-arrow was clicked, null otherwise
      let _backTriggered = false; // guards against multiple history.back() calls
      let _fallbackTimer = null;

      function close(immediate) {
        if (_closed) return;
        _closed = true;
        if (_fallbackTimer) { clearTimeout(_fallbackTimer); _fallbackTimer = null; }
        if (typeof onClose === 'function') onClose();
        unlockScroll();
        animateCloseOverlay(overlay, immediate);
        document.removeEventListener('keydown', onKey);
        window.removeEventListener('popstate', onPopState);
        openModalRegistry = openModalRegistry.filter(fn => fn !== close);
      }
      function goBack() {
        close();
        if (typeof onBack === 'function') onBack();
      }

      // Single entry point for every dismissal path (overlay click, ✕,
      // Escape, back-arrow). Guards against firing history.back() more
      // than once per modal instance — without this, a click registered
      // before the first click's popstate has resolved would queue a
      // second history.back(), popping one entry further than intended
      // (since only one entry was ever pushed for this modal) and landing
      // the user past the underlying page entirely. As a safety net in
      // case popstate never arrives (e.g. nothing left to go back to),
      // fall back to closing directly after a short delay.
      function requestClose(action) {
        if (_closed || _backTriggered) return;
        _backTriggered = true;
        _pendingAction = action;
        history.back();
        _fallbackTimer = setTimeout(() => {
          if (!_closed) {
            if (action === 'back') goBack(); else close();
          }
        }, 400);
      }

      function onPopState() {
        if (_closed) return;
        if (_lightboxPopStateHandled) {
          _lightboxPopStateHandled = false;
          return;
        }
        if (_fallbackTimer) { clearTimeout(_fallbackTimer); _fallbackTimer = null; }
        if (_pendingAction === 'back' || (!_pendingAction && typeof onBack === 'function')) {
          _pendingAction = null;
          goBack();
        } else {
          _pendingAction = null;
          close();
        }
      }

      if (typeof onBack === 'function') {
        const backBtn = document.createElement('button');
        backBtn.className = 'assignable-modal-back';
        backBtn.setAttribute('aria-label', 'Back');
        const arrowIcon = document.createElement('span');
        arrowIcon.className = 'back-arrow';
        backBtn.appendChild(arrowIcon);
        backBtn.addEventListener('click', () => requestClose('back'));
        header.appendChild(backBtn);
      }

      if (title) {
        const h3 = document.createElement('h3');
        h3.className = 'assignable-modal-title';
        h3.textContent = title;
        header.appendChild(h3);
      }

      const spacer = document.createElement('span');
      spacer.style.flex = '1';
      header.appendChild(spacer);

      const closeBtn = document.createElement('button');
      closeBtn.className = 'assignable-modal-close';
      closeBtn.textContent = '✕';
      closeBtn.addEventListener('click', () => requestClose('close'));
      header.appendChild(closeBtn);

      modal.appendChild(header);

      // Measure header height so the sticky header (tabs/search/filter)
      // can position itself directly below it via CSS var(--header-height).
      requestAnimationFrame(() => {
        modal.style.setProperty('--header-height', header.offsetHeight + 'px');
      });

      wrap.appendChild(modal);
      overlay.appendChild(wrap);
      overlay.addEventListener('click', () => requestClose('close'));

      const onKey = e => { if (e.key === 'Escape') requestClose('close'); };
      document.addEventListener('keydown', onKey);

      lockScroll();
      document.body.appendChild(overlay);
      openModalRegistry.push(close);
      window.addEventListener('popstate', onPopState);
      history.pushState({ modal: true }, '');

      function syncOverflow() {
        requestAnimationFrame(() => {
          modal.style.overflowY = modal.scrollHeight > modal.clientHeight + 1 ? 'auto' : 'hidden';
        });
      }
      const _mo = new MutationObserver(syncOverflow);
      _mo.observe(modal, { childList: true, subtree: true });

      return { overlay, wrap, modal, close, syncOverflow };
    }

    // ── Paginated grid helper ──
    // Renders `items` into `grid` in pages, with prev/next controls
    // in `pagerEl`. Page size is computed dynamically from the grid's
    // rendered width: columns × row limit. Desktop gets 15 rows per
    // page, mobile gets 20. If `searchInput` is given, typing in it
    // switches to showing every match across the full list (pagination
    // doesn't apply to search results — the filtered set is usually
    // small); clearing the search restores the paginated view.
    const MODAL_TILE_W = 96;
    const MODAL_TILE_GAP = 8;
    const MODAL_MAX_ROWS_DESKTOP = 15;
    const MODAL_MAX_ROWS_MOBILE = 20;
    const SEARCH_CAP = 200;

    function getModalPageSize(grid) {
      const isMobile = window.matchMedia('(max-width: 600px)').matches;
      const maxRows = isMobile ? MODAL_MAX_ROWS_MOBILE : MODAL_MAX_ROWS_DESKTOP;
      // Measure columns from already-rendered children if available,
      // otherwise fall back to a grid-width estimate.
      const children = grid.children;
      if (children.length > 1) {
        const firstTop = children[0].offsetTop;
        let cols = 1;
        for (let i = 1; i < children.length; i++) {
          if (children[i].offsetTop === firstTop) cols++;
          else break;
        }
        return cols * maxRows;
      }
      const scrollParent = grid.closest('.assignable-modal') || grid.parentElement;
      const available = scrollParent ? scrollParent.clientWidth - 40 : grid.offsetWidth;
      const cols = Math.max(1, Math.floor((available + MODAL_TILE_GAP) / (MODAL_TILE_W + MODAL_TILE_GAP)));
      return cols * maxRows;
    }

    function renderPaginatedGrid({ items, grid, pagerEl, tileFactory, searchInput, filterFn, onChange }) {
      let page = 0;
      let activeFilter = filterFn || null;
      // `baseItems` is the filter-only view (no search applied yet) — this is
      // what search should always narrow from. Previously search narrowed
      // whatever `filteredItems` happened to be, which was itself the
      // *previous* search's result set. That meant once a search produced
      // zero matches, every subsequent keystroke (including deleting back
      // toward a valid query) kept searching within that empty set and could
      // never recover results without clearing the box entirely.
      let baseItems = activeFilter ? items.filter(activeFilter) : items;
      let filteredItems = baseItems;

      // Pre-lowercase names once so filtering avoids repeated toLowerCase()
      items.forEach(it => { it._searchName = (it.Name || '').toLowerCase(); });

      function totalPages() { return Math.max(1, Math.ceil(filteredItems.length / getModalPageSize(grid))); }

      function renderPage() {
        // Measure columns before clearing so we count actual rendered items
        const ps = getModalPageSize(grid);
        grid.replaceChildren();
        const start = page * ps;
        const frag = document.createDocumentFragment();
        filteredItems.slice(start, start + ps).forEach(it => frag.appendChild(tileFactory(it)));
        grid.appendChild(frag);
        updatePagerUI();

        // Fade-in animation on page swap
        grid.style.transition = 'none';
        grid.style.opacity = '0';
        requestAnimationFrame(() => {
          requestAnimationFrame(() => {
            grid.style.transition = 'opacity 0.18s ease';
            grid.style.opacity = '1';
          });
        });

        // Auto-scroll the nearest scrollable parent (the modal) back to top
        const scrollParent = grid.closest('.assignable-modal') || grid.closest('.assignable-modal-wrap');
        if (scrollParent) {
          scrollParent.scrollTop = 0;
        }

        if (typeof onChange === 'function') onChange();
      }

      function updatePagerUI() {
        const tp = totalPages();
        pagerEl.style.display = 'flex';
        prevBtn.disabled = page === 0;
        nextBtn.disabled = page >= tp - 1;
        label.textContent = `Page ${page + 1} of ${tp}`;
      }

      pagerEl.replaceChildren();
      const prevBtn = document.createElement('button');
      prevBtn.type = 'button';
      prevBtn.className = 'modal-pager-btn';
      prevBtn.textContent = '‹';
      prevBtn.setAttribute('aria-label', 'Previous page');
      prevBtn.addEventListener('click', () => { if (page > 0) { page--; renderPage(); } });

      const label = document.createElement('span');
      label.className = 'modal-pager-label';

      const nextBtn = document.createElement('button');
      nextBtn.type = 'button';
      nextBtn.className = 'modal-pager-btn';
      nextBtn.textContent = '›';
      nextBtn.setAttribute('aria-label', 'Next page');
      nextBtn.addEventListener('click', () => { if (page < totalPages() - 1) { page++; renderPage(); } });

      pagerEl.appendChild(prevBtn);
      pagerEl.appendChild(label);
      pagerEl.appendChild(nextBtn);

      function applySearch() {
        const q = searchInput ? searchInput.value.toLowerCase() : '';
        if (q) {
          // Always search from baseItems (the un-searched, filter-only set),
          // never from the previous search's already-narrowed filteredItems.
          const matches = [];
          for (let i = 0; i < baseItems.length; i++) {
            if (baseItems[i]._searchName.includes(q)) matches.push(baseItems[i]);
          }
          const searchPageTotal = Math.max(1, Math.ceil(matches.length / getModalPageSize(grid)));
          page = 0;
          filteredItems = matches;
          renderPage();
          label.textContent = `Page 1 of ${searchPageTotal}`;
          prevBtn.disabled = true;
          nextBtn.disabled = searchPageTotal <= 1;
          pagerEl.style.display = 'flex';
        } else {
          filteredItems = baseItems;
          page = 0;
          renderPage();
        }
      }

      if (searchInput) {
        let searchTimer = null;
        searchInput.addEventListener('input', (e) => {
          clearTimeout(searchTimer);
          if (e.isTrusted) {
            searchTimer = setTimeout(applySearch, 150);
          } else {
            applySearch();
          }
        });
      }

      renderPage();

      return {
        setFilter(fn) {
          activeFilter = fn;
          baseItems = fn ? items.filter(fn) : items;
          page = 0;
          applySearch();
        },
        getPage() { return page; },
        setPage(p) { page = Math.max(0, p); renderPage(); }
      };
    }

    // ── Fixed-frame height lock for single-grid "view more" modals ──
    // Locks the MODAL's own outer box height to its first, unfiltered
    // render, then holds it fixed. Search can only narrow the result set,
    // so that first render is already the tallest this modal will ever
    // need to be — and since the browser has already clamped it to the
    // modal's own CSS max-height by the time it's measured, the locked
    // value can never exceed what's actually visible on screen. (An
    // earlier version of this locked the grid's own min-height instead,
    // which had a real bug: if the full unfiltered list was itself taller
    // than max-height — i.e. it already needed scrolling to see it all —
    // then a later search narrowing things down to a couple of items still
    // left the grid claiming all that vertical space, so the modal stayed
    // scrollable through what was now mostly blank filler.)
    // Returns a function to pass as `onChange` to renderPaginatedGrid.
    function createHeightLock(modal, syncOverflow) {
      let locked = false;
      return function syncHeight() {
        if (!locked) {
          const h = modal.getBoundingClientRect().height;
          if (h > 0) {
            modal.style.height = h + 'px';
            locked = true;
          }
        }
        if (typeof syncOverflow === 'function') syncOverflow();
      };
    }

    function slugify(text) {
      return String(text || '')
        .normalize('NFKD')
        .replace(/\p{Diacritic}/gu, '')
        .replace(/[^a-zA-Z0-9]+/g, '-')
        .replace(/(^-|-$)/g, '');
    }

    // ── Responsive preview grid trimming ──
    // After the layout is in the DOM, measures each preview grid's real
    // rendered width to compute column count, then trims it to exactly
    // maxRows complete rows. The last slot of the final row becomes a
    // "+X more" tile. Runs post-layout so offsetWidth is accurate.
    const PREVIEW_TILE_W = 96;
    const PREVIEW_GAP = 8;
    const _previewGrids = []; // { grid, total, moreClass, moreFn, moreType }
    function registerPreviewGrid(grid, total, moreClass, moreFn, moreType) {
      _previewGrids.push({ grid, total, moreClass, moreFn, moreType });
    }
    function trimPreviewGrids() {
      const maxRows = window.matchMedia('(max-width: 600px)').matches ? 3 : 2;
      _previewGrids.forEach(({ grid, total, moreClass, moreFn, moreType }) => {
        const cols = Math.max(1, Math.floor((grid.offsetWidth + PREVIEW_GAP) / (PREVIEW_TILE_W + PREVIEW_GAP)));
        const limit = cols * maxRows;
        if (total <= limit) return; // everything fits, no "+more" needed
        const visibleCount = limit - 1; // last slot is the "+more" tile
        const children = Array.from(grid.children);
        // Remove excess children (keep only visibleCount)
        for (let i = children.length - 1; i >= visibleCount; i--) {
          grid.removeChild(children[i]);
        }
        // Append "+X more" tile
        const more = document.createElement('div');
        more.className = moreClass;
        const overflow = total - visibleCount;
        more.textContent = `+${overflow} more`;
        more.addEventListener('click', moreFn);
        grid.appendChild(more);
      });
      _previewGrids.length = 0;
    }

    function buildItemViewUrl(item) {
      const formId = item && item.CNAM_FormID ? item.CNAM_FormID : item && item.FormID ? item.FormID : '';
      const name = item && item.Name ? item.Name : '';
      const slug = slugify(name);
      return formId ? `item-view.html#${encodeURIComponent(slug || 'item')}/${encodeURIComponent(formId)}` : 'item-view.html';
    }

    function parseItemViewRoute() {
      const hash = window.location.hash.startsWith('#') ? window.location.hash.slice(1) : '';
      const hashParts = hash.split('/').filter(Boolean);
      const params = new URLSearchParams(window.location.search);
      const idParam = params.get('id');
      if (hashParts.length >= 2) {
        const formId = decodeURIComponent(hashParts[hashParts.length - 1] || '');
        const slug = decodeURIComponent(hashParts[hashParts.length - 2] || '');
        return { id: formId || idParam || '', slug };
      }
      return { id: idParam || '', slug: '' };
    }

    // ── Navigation history (prev/next within item-view) ──
    const NAV_KEY = 'itemViewHistory';

    // If arriving fresh from index.html, wipe any stale history first
    const _initParams = new URLSearchParams(window.location.search);
    if (_initParams.get('fresh') === '1') {
      sessionStorage.removeItem(NAV_KEY);
      sessionStorage.removeItem(NAV_KEY + '_idx');
      const route = parseItemViewRoute();
      const cleanUrl = route.id ? `item-view.html#${route.slug ? `${encodeURIComponent(route.slug)}/` : ''}${encodeURIComponent(route.id)}` : 'item-view.html';
      history.replaceState(null, '', cleanUrl);
    }

    let navHistory = JSON.parse(sessionStorage.getItem(NAV_KEY) || '[]')
      // Normalize any legacy {id,cat} objects from an earlier experiment back to plain strings.
      .map(h => (h && typeof h === 'object' ? h.id : h));
    let navIndex   = parseInt(sessionStorage.getItem(NAV_KEY + '_idx') || '-1', 10);

    function updateNavButtons(formId) {
      const prev = document.getElementById('navPrev');
      const next = document.getElementById('navNext');
      if (navIndex > 0) {
        prev.classList.add('active');
        prev.href = `item-view.html#item/${encodeURIComponent(navHistory[navIndex - 1])}`;
        prev.onclick = e => { e.preventDefault(); navIndex--; sessionStorage.setItem(NAV_KEY + '_idx', navIndex); window.location.assign(prev.href); };
      } else {
        prev.classList.remove('active');
        prev.removeAttribute('href');
      }
      if (navIndex < navHistory.length - 1) {
        next.classList.add('active');
        next.href = `item-view.html#item/${encodeURIComponent(navHistory[navIndex + 1])}`;
        next.onclick = e => { e.preventDefault(); navIndex++; sessionStorage.setItem(NAV_KEY + '_idx', navIndex); window.location.assign(next.href); };
      } else {
        next.classList.remove('active');
        next.removeAttribute('href');
      }
    }

    // ── Load ──
    const contentDiv = document.getElementById('content');
    let activeRouteToken = 0;

    // Cache the DB/AssignableInfo fetches across item navigations (prev/next,
    // shared-item clicks, assignable-item clicks all re-run loadItemRoute via
    // hashchange) so we don't re-download + re-parse the whole database on
    // every single click.
    let dbPromise = null;
    let assignableInfoPromise = null;
    let playerBuffInfoPromise = null;

    function loadItemRoute() {
      closeAllModals();
      clearDetailTooltips();

      // Hide the current content *instantly* (skip the transition) before
      // jumping the scroll position back to top. Without this, scrollTo(0,0)
      // fires while the previous item's content is still fully visible,
      // producing a jarring flash of the old page at the top for a frame
      // before it clears — worst when navigating from a shared-item tile
      // near the bottom of a long detail page. Hiding first means the jump
      // happens while contentDiv is already invisible, so it's never seen.
      contentDiv.classList.remove('detail-content-ready');
      contentDiv.style.transition = 'none';
      contentDiv.classList.add('detail-content-loading');
      void contentDiv.offsetWidth; // flush the instant hide
      contentDiv.style.transition = '';
      window.scrollTo(0, 0);

      const route = parseItemViewRoute();
      const formId = route.id;
      const currentToken = ++activeRouteToken;

      contentDiv.replaceChildren();

      if (!formId) {
        contentDiv.innerHTML = '<div class="error-message">Error: No item ID provided.</div>';
        return;
      }

      // Update history: if navigating via prev/next the index is already set;
      // if arriving fresh (e.g. from shared-item tile or main DB), push to history
      const arrivedViaPrevNext = navHistory[navIndex] === formId;
      if (!arrivedViaPrevNext) {
        // Truncate forward history and push new entry
        navHistory = navHistory.slice(0, navIndex + 1);
        navHistory.push(formId);
        navIndex = navHistory.length - 1;
        sessionStorage.setItem(NAV_KEY, JSON.stringify(navHistory));
        sessionStorage.setItem(NAV_KEY + '_idx', navIndex);
      }
      updateNavButtons(formId);

      Promise.all([
        (dbPromise || (dbPromise = fetch('final_workshop_db.json').then(r => r.json()))),
        (assignableInfoPromise || (assignableInfoPromise = fetch('AssignableInfo.json').then(r => r.json()).catch(() => ({})))),
        (playerBuffInfoPromise || (playerBuffInfoPromise = fetch('PlayerBuffInfo.json').then(r => r.json()).catch(() => ({}))))
      ])
        .then(([data, assignableInfo, playerBuffInfo]) => {
          if (currentToken !== activeRouteToken) return;
          const item = data.find(i => i.CNAM_FormID === formId);
          if (!item) {
            contentDiv.classList.remove('detail-content-loading');
            contentDiv.classList.add('detail-content-ready');
            contentDiv.innerHTML = `<div class="error-message">Item "${formId}" not found.</div>`;
            return;
          }
          const currentUrl = `${window.location.pathname}${window.location.search}${window.location.hash}`;
          const cleanUrl = `${window.location.pathname}${window.location.search}${buildItemViewUrl(item).includes('#') ? buildItemViewUrl(item).slice(buildItemViewUrl(item).indexOf('#')) : ''}`;
          if (currentUrl !== cleanUrl) {
            history.replaceState(null, '', cleanUrl);
          }
          renderDetailPage(item, data, assignableInfo, playerBuffInfo);
        })
        .catch(err => {
          dbPromise = null;
          assignableInfoPromise = null;
          playerBuffInfoPromise = null;
          if (currentToken !== activeRouteToken) return;
          console.error(err);
          contentDiv.classList.remove('detail-content-loading');
          contentDiv.classList.add('detail-content-ready');
          contentDiv.innerHTML = '<div class="error-message">Error loading database.</div>';
        });
    }

    window.addEventListener('hashchange', loadItemRoute);
    loadItemRoute();

    function fadeImg(img) {
      img.classList.add('lazy-fade');
      if (img.complete && img.naturalWidth > 0) {
        img.classList.add('loaded');
      } else {
        img.addEventListener('load', () => img.classList.add('loaded'), { once: true });
        img.addEventListener('error', () => img.classList.add('loaded'), { once: true });
      }
      return img;
    }

    // ── Shared zoom/pan behavior for an image viewport ──
    // Wires up click-to-zoom (cycling through ZOOM_SCALES) and drag-to-pan
    // once zoomed in, on any `.lightbox-image-viewport`-style container.
    // Cursor stays a zoom cursor at all times (zoom-in while there's more
    // room to zoom, zoom-out once at max) — it only switches to a grab
    // hand while the mouse button is actually held down and dragging (see
    // .dragging in CSS). Returns { resetView, consumeSuppress, cleanup }
    // so callers can reset zoom on image change, avoid a stray click
    // closing things right after a drag-release, and remove the
    // document-level drag listeners when done.
    function attachZoomPan(viewport, img) {
      const ZOOM_SCALES = [1, 1.5, 2.5];
      let zoomLevel = 0;
      let panX = 0;
      let panY = 0;
      let isDragging = false;
      let dragMoved = false;
      let suppressNextClick = false;
      let dragStartX = 0;
      let dragStartY = 0;
      let panStartX = 0;
      let panStartY = 0;
      const DRAG_THRESHOLD = 4;
      const OVERDRAG_BUFFER = 40;

      // Pinch zoom state
      let isPinching = false;
      let pinchStartDist = 0;
      let pinchStartScale = 1;
      let pinchStartPanX = 0;
      let pinchStartPanY = 0;
      let pinchMidX = 0;
      let pinchMidY = 0;
      const PINCH_MIN = 1;
      const PINCH_MAX = 5;

      // Touch-pan state (single finger when zoomed in)
      let isTouchPanning = false;
      let touchPanStartX = 0;
      let touchPanStartY = 0;
      let touchPanOrigX = 0;
      let touchPanOrigY = 0;
      let touchPanMoved = false;

      img.style.transformOrigin = 'center center';

      function applyTransform() {
        const scale = ZOOM_SCALES[zoomLevel] || 1;
        img.style.transform = `translate(${panX}px, ${panY}px) scale(${scale})`;
        viewport.classList.toggle('zoomed-1', zoomLevel === 1);
        viewport.classList.toggle('zoomed-2', zoomLevel === 2);
        viewport.classList.toggle('zoomed-active', scale > 1);
      }

      function resetView() {
        zoomLevel = 0;
        panX = 0;
        panY = 0;
        applyTransform();
      }

      function getCurrentScale() {
        return ZOOM_SCALES[zoomLevel] || 1;
      }

      function clampPan(x, y, scale) {
        if (scale == null) scale = getCurrentScale();
        const rect = viewport.getBoundingClientRect();
        const maxX = (rect.width * (scale - 1)) / 2 + OVERDRAG_BUFFER;
        const maxY = (rect.height * (scale - 1)) / 2 + OVERDRAG_BUFFER;
        return [Math.max(-maxX, Math.min(maxX, x)), Math.max(-maxY, Math.min(maxY, y))];
      }

      function zoomToPoint(clientX, clientY) {
        const scale = getCurrentScale();
        const rect = viewport.getBoundingClientRect();
        const offsetX = (clientX - rect.left) - rect.width / 2;
        const offsetY = (clientY - rect.top) - rect.height / 2;
        [panX, panY] = clampPan(offsetX * (1 - scale), offsetY * (1 - scale));
        applyTransform();
      }

      function onMouseDown(e) {
        if (zoomLevel === 0) return;
        e.preventDefault();
        isDragging = true;
        dragMoved = false;
        dragStartX = e.clientX;
        dragStartY = e.clientY;
        panStartX = panX;
        panStartY = panY;
        img.style.transition = 'none';
        viewport.classList.add('dragging');
      }

      function onDragMove(e) {
        if (!isDragging) return;
        const dx = e.clientX - dragStartX;
        const dy = e.clientY - dragStartY;
        if (!dragMoved && Math.hypot(dx, dy) > DRAG_THRESHOLD) {
          dragMoved = true;
        }
        if (dragMoved) {
          [panX, panY] = clampPan(panStartX + dx, panStartY + dy);
          applyTransform();
        }
      }

      function onDragEnd() {
        if (!isDragging) return;
        isDragging = false;
        viewport.classList.remove('dragging');
        img.style.transition = '';
        if (dragMoved) {
          suppressNextClick = true;
        }
      }

      function onViewportClick(e) {
        if (suppressNextClick) {
          suppressNextClick = false;
          return;
        }
        const maxLevel = ZOOM_SCALES.length - 1;
        if (zoomLevel >= maxLevel) {
          resetView();
        } else {
          zoomLevel++;
          zoomToPoint(e.clientX, e.clientY);
        }
      }

      // ── Pinch-to-zoom (mobile) ──
      function getPinchDist(t1, t2) {
        return Math.hypot(t2.clientX - t1.clientX, t2.clientY - t1.clientY);
      }
      function getPinchMid(t1, t2) {
        return { x: (t1.clientX + t2.clientX) / 2, y: (t1.clientY + t2.clientY) / 2 };
      }
      function findNearestLevel(scale) {
        let best = 0;
        let bestDiff = Infinity;
        for (let i = 0; i < ZOOM_SCALES.length; i++) {
          const diff = Math.abs(ZOOM_SCALES[i] - scale);
          if (diff < bestDiff) { bestDiff = diff; best = i; }
        }
        return best;
      }

      function onTouchStart(e) {
        if (e.touches.length === 2) {
          e.preventDefault();
          e.stopPropagation();
          isPinching = true;
          isTouchPanning = false;
          isDragging = false;
          pinchStartDist = getPinchDist(e.touches[0], e.touches[1]);
          pinchStartScale = getCurrentScale();
          pinchStartPanX = panX;
          pinchStartPanY = panY;
          const mid = getPinchMid(e.touches[0], e.touches[1]);
          pinchMidX = mid.x;
          pinchMidY = mid.y;
          img.style.transition = 'none';
        } else if (e.touches.length === 1 && getCurrentScale() > 1) {
          e.preventDefault();
          e.stopPropagation();
          isTouchPanning = true;
          touchPanMoved = false;
          touchPanStartX = e.touches[0].clientX;
          touchPanStartY = e.touches[0].clientY;
          touchPanOrigX = panX;
          touchPanOrigY = panY;
          img.style.transition = 'none';
          viewport.classList.add('dragging');
        }
      }

      function onTouchMove(e) {
        if (isPinching && e.touches.length === 2) {
          e.preventDefault();
          e.stopPropagation();
          const dist = getPinchDist(e.touches[0], e.touches[1]);
          const scaleRatio = dist / pinchStartDist;
          const newScale = Math.max(PINCH_MIN, Math.min(PINCH_MAX, pinchStartScale * scaleRatio));

          const mid = getPinchMid(e.touches[0], e.touches[1]);
          const dx = mid.x - pinchMidX;
          const dy = mid.y - pinchMidY;
          const [cx, cy] = clampPan(pinchStartPanX + dx, pinchStartPanY + dy, newScale);

          img.style.transform = `translate(${cx}px, ${cy}px) scale(${newScale})`;
          viewport.classList.toggle('zoomed-1', newScale >= 1.5);
          viewport.classList.toggle('zoomed-2', newScale >= 2.5);
          viewport.classList.toggle('zoomed-active', newScale > 1);
        } else if (isTouchPanning && e.touches.length === 1) {
          e.preventDefault();
          e.stopPropagation();
          const dx = e.touches[0].clientX - touchPanStartX;
          const dy = e.touches[0].clientY - touchPanStartY;
          if (!touchPanMoved && Math.hypot(dx, dy) > DRAG_THRESHOLD) {
            touchPanMoved = true;
          }
          if (touchPanMoved) {
            const [cx, cy] = clampPan(touchPanOrigX + dx, touchPanOrigY + dy);
            panX = cx;
            panY = cy;
            applyTransform();
          }
        }
      }

      function onTouchEnd(e) {
        if (isPinching) {
          isPinching = false;
          img.style.transition = '';

          const transform = img.style.transform;
          const scaleMatch = transform.match(/scale\(([^)]+)\)/);
          const finalScale = scaleMatch ? parseFloat(scaleMatch[1]) : 1;
          const txMatch = transform.match(/translate\(([^,]+)px,\s*([^)]+)px\)/);
          const finalTx = txMatch ? parseFloat(txMatch[1]) : 0;
          const finalTy = txMatch ? parseFloat(txMatch[2]) : 0;

          zoomLevel = findNearestLevel(finalScale);
          if (zoomLevel === 0) {
            resetView();
          } else {
            panX = finalTx;
            panY = finalTy;
            applyTransform();
          }
          suppressNextClick = true;
        } else if (isTouchPanning) {
          isTouchPanning = false;
          img.style.transition = '';
          viewport.classList.remove('dragging');
          if (touchPanMoved) {
            suppressNextClick = true;
          }
        }
      }

      viewport.addEventListener('mousedown', onMouseDown);
      viewport.addEventListener('click', onViewportClick);
      viewport.addEventListener('touchstart', onTouchStart, { passive: false });
      viewport.addEventListener('touchmove', onTouchMove, { passive: false });
      viewport.addEventListener('touchend', onTouchEnd, { passive: true });

      // ── Double-tap to zoom (mobile) ──
      {
        let lastTapTime = 0;
        let lastTapX = 0;
        let lastTapY = 0;
        viewport.addEventListener('touchend', e => {
          if (e.touches.length > 0) return;
          if (isPinching) return;
          e.preventDefault();
          const now = Date.now();
          const x = e.changedTouches[0].clientX;
          const y = e.changedTouches[0].clientY;
          if (now - lastTapTime < 300 && Math.abs(x - lastTapX) < 30 && Math.abs(y - lastTapY) < 30) {
            if (zoomLevel > 0) {
              resetView();
            } else {
              zoomLevel = 1;
              zoomToPoint(x, y);
            }
            lastTapTime = 0;
          } else {
            lastTapTime = now;
            lastTapX = x;
            lastTapY = y;
          }
        }, { passive: false });
      }

      document.addEventListener('mousemove', onDragMove);
      document.addEventListener('mouseup', onDragEnd);

      function cleanup() {
        document.removeEventListener('mousemove', onDragMove);
        document.removeEventListener('mouseup', onDragEnd);
      }

      function consumeSuppress() {
        const was = suppressNextClick;
        suppressNextClick = false;
        return was;
      }

      applyTransform();

      return {
        resetView,
        cleanup,
        consumeSuppress,
        get zoomLevel() { return zoomLevel; },
        get isZoomed() { return getCurrentScale() > 1; },
        onKeySpace() {
          const maxLevel = ZOOM_SCALES.length - 1;
          if (zoomLevel >= maxLevel) resetView(); else { zoomLevel++; applyTransform(); }
        },
        zoomIn(centerX, centerY) {
          const maxLevel = ZOOM_SCALES.length - 1;
          if (zoomLevel < maxLevel) {
            zoomLevel++;
            if (centerX != null && centerY != null) zoomToPoint(centerX, centerY);
            else applyTransform();
          }
        },
        zoomOut() {
          if (zoomLevel > 0) {
            zoomLevel--;
            if (zoomLevel === 0) resetView(); else applyTransform();
          }
        }
      };
    }

    function openLightbox(startImage, onBack) {
      const overlay = document.createElement('div');
      overlay.className = 'lightbox-overlay';

      const isMainRender = startImage && startImage.isMainRender;
      const galleryRoot = !isMainRender && startImage && startImage.closest
        ? startImage.closest('.storefront-grid, .model-renders-grid')
        : null;
      let allImages;
      let startIndex = 0;
      if (isMainRender) {
        allImages = [startImage];
      } else {
        allImages = galleryRoot
          ? Array.from(galleryRoot.querySelectorAll('.storefront-thumb img'))
          : [startImage];
        startIndex = allImages.indexOf(startImage) >= 0 ? allImages.indexOf(startImage) : 0;
      }
      let activeIndex = startIndex;
      const isGallery = !isMainRender && allImages.length > 1;

      const _preloads = [];
      let _idleTimer = null;

      // ── Close helpers ──
      let _lbClosed = false;
      let _lbBackTriggered = false; // guards backBtn against firing history.back() twice
      function closeLightboxImmediate() {
        if (_lbClosed) return;
        _lbClosed = true;
        clearTimeout(_idleTimer);
        _preloads.forEach(l => l.remove());
        overlay.style.transform = '';
        overlay.style.opacity = '';
        overlay.style.transition = '';
        unlockScroll();
        zoomPan.cleanup();
        document.removeEventListener('keydown', onKey);
        window.removeEventListener('popstate', onLbPopState);
        animateCloseOverlay(overlay, true);
      }
      function closeLightboxSoft() {
        if (_lbClosed) return;
        _lbClosed = true;
        clearTimeout(_idleTimer);
        _preloads.forEach(l => l.remove());
        overlay.style.transform = '';
        overlay.style.opacity = '';
        overlay.style.transition = '';
        unlockScroll();
        zoomPan.cleanup();
        document.removeEventListener('keydown', onKey);
        window.removeEventListener('popstate', onLbPopState);
        animateCloseOverlay(overlay);
      }
      function closeAllAndHistory() {
        closeLightboxImmediate();
        closeAllModals();
        history.go(-1);
      }

      // ── Header bar: back + counter + close ──
      const header = document.createElement('div');
      header.className = 'lightbox-header';

      if (onBack) {
        _lightboxPopStateHandled = true;
        history.pushState({ lightbox: true }, '');
        const backBtn = document.createElement('button');
        backBtn.className = 'assignable-modal-back';
        backBtn.setAttribute('aria-label', 'Back');
        const arrowIcon = document.createElement('span');
        arrowIcon.className = 'back-arrow';
        backBtn.appendChild(arrowIcon);
        backBtn.addEventListener('click', e => {
          e.stopPropagation();
          if (_lbBackTriggered) return;
          _lbBackTriggered = true;
          closeLightboxSoft();
          history.back();
        });
        header.appendChild(backBtn);
      }

      const counterEl = document.createElement('span');
      counterEl.className = 'lightbox-counter';

      const closeBtn = document.createElement('button');
      closeBtn.className = 'lightbox-close';
      closeBtn.textContent = '✕';
      closeBtn.setAttribute('aria-label', 'Close');
      closeBtn.addEventListener('click', () => {
        if (onBack) { closeAllAndHistory(); } else { closeLightboxSoft(); }
      });

      header.appendChild(counterEl);
      header.appendChild(closeBtn);

      // ── Main stage: image + nav arrows ──
      const stage = document.createElement('div');
      stage.className = 'lightbox-stage';
      // Clicks are "inside" (shouldn't close the lightbox) when they land
      // within the image's actual current rendered bounds — not the static
      // stage box. This matters once zoomed in: the image can extend past
      // the stage's box (see .lightbox-stage overflow:visible), so the
      // "click outside closes" hit-test has to track the image's real,
      // possibly-larger footprint rather than a fixed rectangle.
      function isPointInsideImage(x, y) {
        const r = img.getBoundingClientRect();
        return x >= r.left && x <= r.right && y >= r.top && y <= r.bottom;
      }
      stage.addEventListener('click', e => {
        if (isPointInsideImage(e.clientX, e.clientY)) {
          e.stopPropagation();
        }
        // Otherwise let it bubble to the overlay's click handler, which
        // closes the lightbox.
      });

      const viewport = document.createElement('div');
      viewport.className = 'lightbox-image-viewport';

      const img = document.createElement('img');
      img.src = isMainRender ? startImage.src : (allImages[activeIndex]?.src || startImage?.src || '');
      img.alt = isMainRender ? (startImage.alt || '') : (allImages[activeIndex]?.alt || '');
      img.loading = 'eager';
      img.draggable = false;

      const zoomPan = attachZoomPan(viewport, img);

      // ── Loading spinner ──
      const spinner = document.createElement('div');
      spinner.className = 'lightbox-spinner';
      viewport.appendChild(spinner);
      img.addEventListener('load', () => { spinner.style.display = 'none'; });
      if (img.complete) spinner.style.display = 'none';

      let sliding = false;
      function waitForTransition(el) {
        return new Promise(resolve => {
          let done = false;
          const onEnd = () => { if (done) return; done = true; el.removeEventListener('transitionend', onEnd); resolve(); };
          el.addEventListener('transitionend', onEnd, { once: true });
          setTimeout(onEnd, 300);
        });
      }

      function showImage(index, direction) {
        if (!isGallery) return;
        const newIndex = (index + allImages.length) % allImages.length;
        if (newIndex === activeIndex) return;
        if (sliding) return;

        if (!direction) {
          activeIndex = newIndex;
          spinner.style.display = '';
          img.src = allImages[activeIndex].src;
          img.alt = allImages[activeIndex].alt || '';
          zoomPan.resetView();
          updateHeader();
          refreshPreloads();
          return;
        }

        sliding = true;

        if (direction === 'fade') {
          img.style.transition = 'opacity 180ms ease';
          img.style.opacity = '0';
          setTimeout(() => {
            activeIndex = newIndex;
            spinner.style.display = '';
            img.src = allImages[activeIndex].src;
            img.alt = allImages[activeIndex].alt || '';
            zoomPan.resetView();
            updateHeader();
            refreshPreloads();
            img.style.opacity = '1';
            setTimeout(() => { img.style.transition = ''; sliding = false; }, 180);
          }, 180);
          return;
        }

        const goingLeft = direction === 'left';
        stage.style.willChange = 'transform';

        stage.style.transition = 'transform 250ms cubic-bezier(0.4, 0, 0.2, 1)';
        stage.style.transform = `translateX(${goingLeft ? '-100%' : '100%'})`;

        waitForTransition(stage).then(() => {
          activeIndex = newIndex;
          spinner.style.display = '';
          img.src = allImages[activeIndex].src;
          img.alt = allImages[activeIndex].alt || '';
          zoomPan.resetView();
          updateHeader();
          refreshPreloads();

          stage.style.transition = 'none';
          stage.style.transform = `translateX(${goingLeft ? '100%' : '-100%'})`;
          stage.offsetHeight;

          stage.style.transition = 'transform 250ms cubic-bezier(0.4, 0, 0.2, 1)';
          stage.style.transform = 'translateX(0)';

          waitForTransition(stage).then(() => {
            stage.style.transition = '';
            stage.style.transform = '';
            stage.style.willChange = '';
            sliding = false;
          });
        });
      }

      function updateHeader() {
        if (isGallery) {
          counterEl.textContent = `${activeIndex + 1} / ${allImages.length}`;
          counterEl.style.display = '';
        } else {
          counterEl.style.display = 'none';
        }
      }
      updateHeader();

      // ── Navigation arrows ──
      const chevronLeft = '<svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="15 6 9 12 15 18"></polyline></svg>';
      const chevronRight = '<svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 6 15 12 9 18"></polyline></svg>';

      const prevBtn = document.createElement('button');
      prevBtn.className = 'lightbox-nav lightbox-nav-prev';
      prevBtn.innerHTML = chevronLeft;
      prevBtn.setAttribute('aria-label', 'Previous image');
      prevBtn.addEventListener('click', e => {
        e.stopPropagation();
        showImage(activeIndex - 1, 'fade');
      });

      const nextBtn = document.createElement('button');
      nextBtn.className = 'lightbox-nav lightbox-nav-next';
      nextBtn.innerHTML = chevronRight;
      nextBtn.setAttribute('aria-label', 'Next image');
      nextBtn.addEventListener('click', e => {
        e.stopPropagation();
        showImage(activeIndex + 1, 'fade');
      });

      // ── Assemble DOM ──
      viewport.appendChild(img);
      if (isGallery) {
        stage.appendChild(prevBtn);
        stage.appendChild(nextBtn);
      }
      stage.appendChild(viewport);
      overlay.appendChild(header);
      overlay.appendChild(stage);

      overlay.addEventListener('click', () => {
        if (zoomPan.consumeSuppress()) return;
        if (onBack) { closeAllAndHistory(); } else { closeLightboxSoft(); }
      });

      // ── Mobile: tap overlay to close ──
      {
        let tapStartX = 0;
        let tapStartY = 0;
        overlay.addEventListener('touchstart', e => {
          if (e.touches.length !== 1) return;
          tapStartX = e.touches[0].clientX;
          tapStartY = e.touches[0].clientY;
        }, { passive: true });
        overlay.addEventListener('touchend', e => {
          // Don't intercept touches on buttons/links/image viewport — let their handlers fire
          if (e.target.closest('button, a, .lightbox-image-viewport')) return;
          const dx = Math.abs(e.changedTouches[0].clientX - tapStartX);
          const dy = Math.abs(e.changedTouches[0].clientY - tapStartY);
          if (dx < 10 && dy < 10) {
            e.preventDefault();
            if (zoomPan.consumeSuppress()) return;
            if (onBack) { closeAllAndHistory(); } else { closeLightboxSoft(); }
          }
        }, { passive: false });
      }

      // ── Swipe to navigate (mobile) ──
      if (isGallery) {
        let touchStartX = 0;
        let touchStartY = 0;
        let swiping = false;
        const SWIPE_THRESHOLD = 50;
        const SWIPE_MAX_Y = 80;
        stage.addEventListener('touchstart', e => {
          if (e.touches.length !== 1) return;
          if (zoomPan.isZoomed) return;
          if (sliding) return;
          touchStartX = e.touches[0].clientX;
          touchStartY = e.touches[0].clientY;
          swiping = true;
        }, { passive: true });
        stage.addEventListener('touchmove', e => {
          if (!swiping) return;
          if (zoomPan.isZoomed) { swiping = false; return; }
          const dx = e.touches[0].clientX - touchStartX;
          const dy = Math.abs(e.touches[0].clientY - touchStartY);
          if (dy > SWIPE_MAX_Y) { swiping = false; return; }
          if (Math.abs(dx) > 10) {
            e.preventDefault();
            stage.style.transition = 'none';
            stage.style.transform = `translateX(${dx}px)`;
          }
        }, { passive: false });
        stage.addEventListener('touchend', e => {
          if (!swiping) return;
          swiping = false;
          if (zoomPan.isZoomed) return;
          const dx = e.changedTouches[0].clientX - touchStartX;
          if (Math.abs(dx) >= SWIPE_THRESHOLD) {
            const dir = dx > 0 ? 'right' : 'left';
            const target = dir === 'left' ? activeIndex + 1 : activeIndex - 1;
            showImage(target, dir);
          } else if (Math.abs(dx) > 5) {
            sliding = true;
            stage.style.transition = 'transform 250ms ease';
            stage.style.transform = 'translateX(0)';
            stage.addEventListener('transitionend', () => {
              stage.style.transition = '';
              stage.style.transform = '';
              sliding = false;
            }, { once: true });
            setTimeout(() => { if (sliding) { stage.style.transition = ''; stage.style.transform = ''; sliding = false; } }, 300);
          }
        }, { passive: true });
      }

      const onKey = e => {
        if (e.key === 'Escape') {
          if (onBack) { closeAllAndHistory(); } else { closeLightboxSoft(); }
        } else if (e.key === 'ArrowLeft' && isGallery) {
          showImage(activeIndex - 1, 'fade');
        } else if (e.key === 'ArrowRight' && isGallery) {
          showImage(activeIndex + 1, 'fade');
        } else if (e.key === ' ') {
          e.preventDefault();
          zoomPan.onKeySpace();
        } else if (e.key === '+' || e.key === '=') {
          e.preventDefault();
          zoomPan.zoomIn();
        } else if (e.key === '-' || e.key === '_') {
          e.preventDefault();
          zoomPan.zoomOut();
        }
      };
      document.addEventListener('keydown', onKey);

      // ── History / popstate (only when opened from a modal) ──
      function onLbPopState() {
        if (_lbClosed) return;
        closeLightboxSoft();
      }
      if (onBack) {
        window.addEventListener('popstate', onLbPopState);
      }

      // ── Preload adjacent images ──
      function refreshPreloads() {
        _preloads.forEach(l => l.remove());
        _preloads.length = 0;
        if (!isGallery) return;
        [activeIndex - 1, activeIndex + 1].forEach(i => {
          const idx = (i + allImages.length) % allImages.length;
          if (idx === activeIndex || !allImages[idx]?.src) return;
          const link = document.createElement('link');
          link.rel = 'preload';
          link.as = 'image';
          link.href = allImages[idx].src;
          document.head.appendChild(link);
          _preloads.push(link);
        });
      }
      refreshPreloads();

      // ── Hide UI on idle (mobile) ──
      const _idleTargets = [header];
      function resetIdle() {
        _idleTargets.forEach(el => el.classList.remove('auto-hidden'));
        clearTimeout(_idleTimer);
        _idleTimer = setTimeout(() => {
          _idleTargets.forEach(el => el.classList.add('auto-hidden'));
        }, 3000);
      }
      overlay.addEventListener('touchstart', resetIdle, { passive: true });
      overlay.addEventListener('mousemove', resetIdle);
      resetIdle();

      // ── Swipe-down to dismiss (mobile) ──
      {
        let swipeStartX = 0;
        let swipeStartY = 0;
        let swipingDown = false;
        overlay.addEventListener('touchstart', e => {
          if (e.touches.length !== 1) return;
          if (e.target.closest('button, a')) return;
          swipeStartX = e.touches[0].clientX;
          swipeStartY = e.touches[0].clientY;
          swipingDown = false;
        }, { passive: true });
        overlay.addEventListener('touchmove', e => {
          if (e.touches.length !== 1) return;
          if (e.target.closest('button, a')) return;
          if (zoomPan.isZoomed) return;
          const dx = Math.abs(e.touches[0].clientX - swipeStartX);
          const dy = e.touches[0].clientY - swipeStartY;
          if (!swipingDown && dy > 10 && dy > dx * 1.5) swipingDown = true;
          if (swipingDown && dy > 0) {
            e.preventDefault();
            overlay.style.transform = `translateY(${dy}px)`;
            overlay.style.opacity = String(Math.max(0, 1 - dy / 400));
          }
        }, { passive: false });
        overlay.addEventListener('touchend', e => {
          if (!swipingDown) return;
          swipingDown = false;
          if (zoomPan.isZoomed) {
            overlay.style.transition = 'transform 250ms ease, opacity 250ms ease';
            overlay.style.transform = 'translateY(0)';
            overlay.style.opacity = '1';
            setTimeout(() => { overlay.style.transition = ''; overlay.style.transform = ''; overlay.style.opacity = ''; }, 260);
            return;
          }
          const dy = e.changedTouches[0].clientY - swipeStartY;
          if (dy > 80) {
            overlay.style.transition = 'transform 250ms ease, opacity 250ms ease';
            overlay.style.transform = 'translateY(100vh)';
            overlay.style.opacity = '0';
            setTimeout(() => {
              if (onBack) { closeAllAndHistory(); } else { closeLightboxSoft(); }
            }, 250);
          } else {
            overlay.style.transition = 'transform 250ms ease, opacity 250ms ease';
            overlay.style.transform = 'translateY(0)';
            overlay.style.opacity = '1';
            setTimeout(() => { overlay.style.transition = ''; overlay.style.transform = ''; overlay.style.opacity = ''; }, 260);
          }
        }, { passive: true });
      }

      lockScroll();
      document.body.appendChild(overlay);
    }

    function updateOGMetaTags(item) {
      const baseUrl = window.location.origin + '/mrsblobby.github.io/76-CAMPDatabase/Experimental/';
      
      // Update title
      document.title = `${item.Name} - C.A.M.P. Database`;
      const titleTag = document.querySelector('meta[property="og:title"]');
      const ogTitle = `${item.Name} - Detailed View`;
      if (titleTag) titleTag.setAttribute('content', ogTitle);
      else {
        const newTag = document.createElement('meta');
        newTag.setAttribute('property', 'og:title');
        newTag.setAttribute('content', ogTitle);
        document.head.appendChild(newTag);
      }

      // Update description
      const description = `${item.Category}${item.SubCategory ? ' > ' + item.SubCategory : ''} | Budget: ${item.BudgetCost || 'N/A'} | Fallout 76 C.A.M.P. Item Database`;
      const descTag = document.querySelector('meta[property="og:description"]');
      if (descTag) descTag.setAttribute('content', description);
      else {
        const newTag = document.createElement('meta');
        newTag.setAttribute('property', 'og:description');
        newTag.setAttribute('content', description);
        document.head.appendChild(newTag);
      }

      // Update image — prefer storefront, fall back to item icon
      let imageUrl = null;
      if (item.ENTM_ImagePath && item.ENTM_ImageNames && item.ENTM_ImageNames.trim()) {
        const basePath = item.ENTM_ImagePath.trim().replace(/\\/g, '/');
        const firstImageName = item.ENTM_ImageNames.split(';')[0].trim().replace(/\.dds$/i, '');
        const relativePath = basePath.replace(/^Textures\//i, '').toLowerCase()
          .replace(/^tgatextures\/atx\//, 'atx/')
          .replace(/^tgaatx\//, 'atx/');
        const imagePath = `storefront/${relativePath}${firstImageName.toLowerCase()}.webp`;
        imageUrl = `https://raw.githubusercontent.com/MrsBlobby/mrsblobby.github.io/refs/heads/main/76-CAMPDatabase/Experimental/${imagePath}`;
      } else {
        const iconFile = (item.ARTO_FormID || item.CNAM_FormID || '').toLowerCase();
        if (iconFile) imageUrl = `../WorkshopIcons/${iconFile}.webp`;
      }
      
      const imageTag = document.querySelector('meta[property="og:image"]');
      if (imageUrl) {
        if (imageTag) imageTag.setAttribute('content', imageUrl);
        else {
          const newTag = document.createElement('meta');
          newTag.setAttribute('property', 'og:image');
          newTag.setAttribute('content', imageUrl);
          document.head.appendChild(newTag);
        }
      }

      // Ensure og:type and og:url exist
      if (!document.querySelector('meta[property="og:type"]')) {
        const typeTag = document.createElement('meta');
        typeTag.setAttribute('property', 'og:type');
        typeTag.setAttribute('content', 'website');
        document.head.appendChild(typeTag);
      }
      if (!document.querySelector('meta[property="og:url"]')) {
        const urlTag = document.createElement('meta');
        urlTag.setAttribute('property', 'og:url');
        urlTag.setAttribute('content', window.location.href);
        document.head.appendChild(urlTag);
      } else {
        document.querySelector('meta[property="og:url"]').setAttribute('content', window.location.href);
      }
    }

    // ── Assignable item detail modal ──
    // Shows Name / EditorID / FormID for an item referenced inside an
    // Assignables list (AssignableInfo.json entries don't have their own
    // detail page). Structured so an image can be dropped in later by
    // reusing the storefront gallery markup (openLightbox above) minus the
    // prev/next controls — see the `imageSrc` hook below.
    // `onBack`, when provided, is called only when the back arrow is
    // clicked — it re-opens the "view more" modal a tile was clicked from.
    // Clicking outside the modal or the ✕ always just closes it.
    function openAssignableItemModal(displayItem, onBack) {
      const { modal } = openModalShell({
        maxWidth: 560,
        title: displayItem.Name || 'Item',
        onBack
      });
      modal.classList.add('assignable-item-modal');

      const imgWrap = document.createElement('div');
      imgWrap.className = 'assignable-item-modal-image';
      imgWrap.style.cssText = 'width:100%;max-width:512px;aspect-ratio:1/1;margin:0 auto 16px;border-radius:4px;overflow:hidden;cursor:pointer;';
      const img = document.createElement('img');
      if (displayItem.StaticRender) {
        img.src = `../ModelRender/${displayItem.StaticRender}`;
      } else {
        const itemType = ((displayItem.ItemType || '').trim() || '_Other').replace(/\//g, '-');
        img.src = `../ModelRender/Assignables/${itemType}/${(displayItem.FormID||'').toUpperCase()}_thumbnail.webp`;
      }
      img.alt = displayItem.Name || '';
      img.draggable = false;
      img.style.cssText = 'width:100%;height:100%;object-fit:contain;';
      img.onerror = function() {
        if (!img._triedLower) {
          img._triedLower = true;
          this.src = `../ModelRender/Assignables/${((displayItem.ItemType || '').trim() || '_Other').replace(/\//g, '-')}/${(displayItem.FormID||'').toLowerCase()}_thumbnail.webp`;
        } else {
          this.onerror = null;
          this.src = '../assets/ImageNotFound.png';
        }
      };
      imgWrap.appendChild(fadeImg(img));
      imgWrap.addEventListener('click', () => {
        openLightbox({ src: img.src, alt: img.alt, isMainRender: true }, () => {});
      });
      modal.appendChild(imgWrap);

      const fieldsWrap = document.createElement('div');
      fieldsWrap.className = 'assignable-item-modal-fields';

      function makeField(label, value, mono) {
        const row = document.createElement('div');
        row.className = 'assignable-item-modal-field';
        row.style.cssText = 'display:flex;justify-content:space-between;align-items:baseline;gap:16px;padding:8px 0;border-bottom:1px solid #383838;font-size:13px;';
        const labelEl = document.createElement('span');
        labelEl.textContent = label;
        labelEl.style.cssText = 'color:var(--text-muted);flex-shrink:0;';
        const valueEl = document.createElement('span');
        valueEl.textContent = value || '—';
        valueEl.style.cssText = 'color:var(--text);' + (mono !== false ? 'font-family:monospace;' : '') + 'text-align:right;word-break:break-all;';
        row.appendChild(labelEl);
        row.appendChild(valueEl);
        return row;
      }

      fieldsWrap.appendChild(makeField('Item Type', displayItem.ItemType, false));
      fieldsWrap.appendChild(makeField('Weight', displayItem.Weight != null ? String(displayItem.Weight) : null, false));
      modal.appendChild(fieldsWrap);

      const techAccordion = document.createElement('div');
      techAccordion.className = 'tech-accordion';

      const techToggle = document.createElement('div');
      techToggle.className = 'tech-accordion-toggle';
      const techLabel = document.createElement('span');
      techLabel.className = 'tech-accordion-label';
      techLabel.textContent = 'Technical';
      const techArrow = document.createElement('span');
      techArrow.className = 'tech-accordion-arrow';
      techToggle.appendChild(techLabel);
      techToggle.appendChild(techArrow);
      techAccordion.appendChild(techToggle);

      const techBody = document.createElement('div');
      techBody.className = 'tech-accordion-body';
      techAccordion.appendChild(techBody);

      techToggle.addEventListener('click', () => {
        const open = techAccordion.classList.toggle('open');
        if (open) {
          techBody.style.maxHeight = techBody.scrollHeight + 'px';
          techBody.addEventListener('transitionend', () => {
            if (techAccordion.classList.contains('open')) techBody.style.maxHeight = 'none';
          }, { once: true });
        } else {
          techBody.style.maxHeight = techBody.scrollHeight + 'px';
          requestAnimationFrame(() => { techBody.style.maxHeight = '0px'; });
        }
      });

      const techFields = document.createElement('div');
      techFields.style.cssText = 'padding: 0 0 4px;';
      techFields.appendChild(makeField('Editor ID', displayItem.EditorID));
      techFields.appendChild(makeField('Form ID', displayItem.FormID));
      techBody.appendChild(techFields);

      modal.appendChild(techAccordion);
    }

    function renderDetailPage(item, db, assignableInfo = [], playerBuffInfo = {}) {
      contentDiv.replaceChildren();
      contentDiv.classList.remove('detail-content-ready');
      contentDiv.classList.add('detail-content-loading');
      updateOGMetaTags(item);
      const cnamFormID = ((item.CNAM_FormID||'')).toLowerCase();
      const artoFormID = ((item.ARTO_FormID||'')).toLowerCase();
      const imgSrc = `../ModelRender/CAMPitem/${cnamFormID.toUpperCase()}_thumbnail.webp`;

      const roundedBudget = item.BudgetCost != null ? Math.round(item.BudgetCost * 100) / 100 : null;
      const budgetIcons = ['','../assets/BudgetBar-Tier1.webp','../assets/BudgetBar-Tier2.webp','../assets/BudgetBar-Tier3.webp','../assets/BudgetBar-Tier4.webp','../assets/BudgetBar-Tier5.webp'];
      const budgetLabels = ['','Very Low','Low','Medium','High','Very High'];
      let budgetTier = 5;
      if (roundedBudget != null) {
        if (roundedBudget < 1) budgetTier=1;
        else if (roundedBudget < 2) budgetTier=2;
        else if (roundedBudget < 4) budgetTier=3;
        else if (roundedBudget < 8) budgetTier=4;
      }

      const layout = document.createElement('div');
      layout.className = 'detail-layout';

      const wrapper = document.createElement('div');
      wrapper.className = 'item-card';

      // ── Infobox ──
      const infobox = document.createElement('div');
      infobox.className = 'infobox';

      const infoboxTitle = document.createElement('div');
      infoboxTitle.className = 'infobox-title';
      infoboxTitle.textContent = item.Name;
      infobox.appendChild(infoboxTitle);

      const imgBox = document.createElement('div');
      imgBox.className = 'infobox-image';
      const img = document.createElement('img');
      img.src = imgSrc; img.alt = item.Name; fadeImg(img);
      // Chain: CNAM upper → CNAM lower → WorkshopIcons(arto) → WorkshopIcons(cnam)
      const artoIconSrc = artoFormID ? `../WorkshopIcons/${artoFormID}.webp` : null;
      const cnamIconSrc = cnamFormID ? `../WorkshopIcons/${cnamFormID}.webp` : null;
      const fbChain = [`../ModelRender/CAMPitem/${cnamFormID}_thumbnail.webp`, artoIconSrc, cnamIconSrc].filter(Boolean);
      let fbIdx = 0;
      img.onerror = function() {
        if (fbIdx < fbChain.length) {
          this.src = fbChain[fbIdx++];
        } else {
          this.onerror = null;
          this.src = '../WorkshopIcons/item-placeholder.webp';
        }
      };
      imgBox.appendChild(img);
      imgBox.addEventListener('click', () => {
        openLightbox({ src: imgSrc, alt: item.Name, isMainRender: true }, () => {});
      });
      infobox.appendChild(imgBox);

      const rows = document.createElement('div');
      rows.className = 'infobox-rows';

      function addRow(label, content) {
        const row = document.createElement('div'); row.className='infobox-row';
        const lbl = document.createElement('div'); lbl.className='infobox-label'; lbl.textContent=label;
        const val = document.createElement('div'); val.className='infobox-value';
        if (typeof content === 'string') val.textContent=content;
        else val.appendChild(content); // works for Element and DocumentFragment
        row.appendChild(lbl); row.appendChild(val); rows.appendChild(row);
      }

      // Budget hero row (full-width, sits between image and the label rows)
      if (roundedBudget != null && (item.Category||'').toLowerCase() !== 'wallpapers') {
        const unitText = roundedBudget===1 ? 'Flamingo Unit' : 'Flamingo Units';
        const budgetHero = document.createElement('div');
        budgetHero.className = 'infobox-budget';
        const budgetLabel = document.createElement('div');
        budgetLabel.className = 'infobox-budget-label';
        budgetLabel.textContent = 'Budget Cost';
        const budgetMain = document.createElement('div');
        budgetMain.className = 'infobox-budget-main';
        const bImg = document.createElement('img');
        bImg.src = budgetIcons[budgetTier];
        bImg.title = budgetLabels[budgetTier];
        budgetMain.appendChild(bImg);
        budgetMain.appendChild(document.createTextNode(`${roundedBudget} ${unitText}`));
        const budgetTierLabel = document.createElement('div');
        budgetTierLabel.className = 'infobox-budget-tier';
        budgetTierLabel.textContent = budgetLabels[budgetTier];
        budgetHero.appendChild(budgetLabel);
        budgetHero.appendChild(budgetMain);
        budgetHero.appendChild(budgetTierLabel);
        infobox.appendChild(budgetHero); // append directly; rows not yet in DOM
      }

      // Interior Shelter Budget hero row
      if (item.ShelterBudget != null) {
        const shelterBudget = item.ShelterBudget;
        const shelterTier = shelterBudget <= 700 ? 1 : shelterBudget < 1500 ? 2 : 3;
        const shelterIcon = `../assets/BudgetBarShelter-Tier${shelterTier}.webp`;
        const shelterLabels = ['', 'Low', 'Medium', 'High'];
        const shelterLabel = shelterLabels[shelterTier];

        const shelterHero = document.createElement('div');
        shelterHero.className = 'infobox-budget';

        const shelterTitle = document.createElement('div');
        shelterTitle.className = 'infobox-budget-label';
        shelterTitle.textContent = 'Interior Shelter Budget';

        const shelterMain = document.createElement('div');
        shelterMain.className = 'infobox-budget-main';
        const sImg = document.createElement('img');
        sImg.src = shelterIcon;
        sImg.title = shelterLabel;
        shelterMain.appendChild(sImg);
        shelterMain.appendChild(document.createTextNode(`${shelterBudget} Flamingo Units`));

        const shelterTierText = document.createElement('div');
        shelterTierText.className = 'infobox-budget-tier';
        shelterTierText.textContent = shelterLabel;

        shelterHero.appendChild(shelterTitle);
        shelterHero.appendChild(shelterMain);
        shelterHero.appendChild(shelterTierText);
        infobox.appendChild(shelterHero);
      }

      // C.A.M.P. Weather Type hero row (in infobox, under budget rows)
      if (item.WeatherKeywords && String(item.WeatherKeywords).trim()) {
        const weatherType = getWeatherType(item.WeatherKeywords);
        const weatherIconMap = {
          'Clear':          '../assets/ClearWeather.png',
          'Rainy':          '../assets/RainyWeather.png',
          'Radstorm / Nuked': '../assets/RadWeather.png',
        };

        const keywords = String(item.WeatherKeywords).split(';').map(k => k.trim()).filter(Boolean);
        const sandstormFlavor = keywords.includes('s_wt_Sandstorm')
          ? '*counts as Sandstorm weather for challenges, which does not affect fishing odds'
          : '';

        const weatherHero = document.createElement('div');
        weatherHero.className = 'infobox-budget';

        const weatherTitle = document.createElement('div');
        weatherTitle.className = 'infobox-budget-label';
        weatherTitle.textContent = 'C.A.M.P. Weather Type (Fishing)';

        const weatherMain = document.createElement('div');
        weatherMain.className = 'infobox-budget-main';
        const wImg = document.createElement('img');
        wImg.src = weatherIconMap[weatherType] || '';
        wImg.title = weatherType;
        weatherMain.appendChild(wImg);
        weatherMain.appendChild(document.createTextNode(weatherType));

        const weatherTierText = document.createElement('div');
        weatherTierText.className = 'infobox-budget-tier';
        weatherTierText.textContent = sandstormFlavor;

        weatherHero.appendChild(weatherTitle);
        weatherHero.appendChild(weatherMain);
        weatherHero.appendChild(weatherTierText);
        infobox.appendChild(weatherHero);
      }

      // ── Collapsible "Technical" section ──
      // Form ID, Editor ID, Model, and Material Swaps are collapsed by
      // default so casual builders see a compact infobox. Power users can
      // expand the drawer to reveal the technical metadata.
      const techAccordion = document.createElement('div');
      techAccordion.className = 'tech-accordion';

      const techToggle = document.createElement('div');
      techToggle.className = 'tech-accordion-toggle';
      const techLabel = document.createElement('span');
      techLabel.className = 'tech-accordion-label';
      techLabel.textContent = 'Technical';
      const techArrow = document.createElement('span');
      techArrow.className = 'tech-accordion-arrow';
      techToggle.appendChild(techLabel);
      techToggle.appendChild(techArrow);
      techAccordion.appendChild(techToggle);

      const techBody = document.createElement('div');
      techBody.className = 'tech-accordion-body';
      techAccordion.appendChild(techBody);

      techToggle.addEventListener('click', () => {
        const open = techAccordion.classList.toggle('open');
        if (open) {
          techBody.style.maxHeight = techBody.scrollHeight + 'px';
          techBody.addEventListener('transitionend', () => {
            if (techAccordion.classList.contains('open')) techBody.style.maxHeight = 'none';
          }, { once: true });
        } else {
          techBody.style.maxHeight = techBody.scrollHeight + 'px';
          requestAnimationFrame(() => { techBody.style.maxHeight = '0px'; });
        }
      });

      // Redirect addRow to the accordion body for technical fields
      function addTechRow(label, content) {
        const row = document.createElement('div'); row.className='infobox-row';
        const lbl = document.createElement('div'); lbl.className='infobox-label'; lbl.textContent=label;
        const val = document.createElement('div'); val.className='infobox-value';
        if (typeof content === 'string') val.textContent=content;
        else val.appendChild(content);
        row.appendChild(lbl); row.appendChild(val); techBody.appendChild(row);
      }

      // Form ID
      if (item.CNAM_FormID) {
        const mono = document.createElement('span');
        mono.style.cssText='font-family:monospace;font-size:13px;'; mono.textContent=item.CNAM_FormID;
        addTechRow('Form ID', mono);
      }

      // Editor ID
      if (item.CNAM_EditorID) {
        const mono = document.createElement('span');
        mono.style.cssText='font-family:monospace;font-size:11px;word-break:break-all;'; mono.textContent=item.CNAM_EditorID;
        addTechRow('Editor ID', mono);
      }

      // Model(s) — singular label for a single model, plural for a
      // composite (multiple NIFs referenced by the record, e.g. PKINs
      // and composite mannequin NPCs). Each row shows just the filename;
      // a "?" reveals the full path (hoverable/copyable, see
      // attachDetailTooltip above).
      const modelPaths = (item.Models || '').split(';').map(s => s.trim()).filter(Boolean);
      if (modelPaths.length) {
        const col = document.createElement('div');
        col.className = 'infobox-value-col';
        modelPaths.forEach(path => {
          const row = document.createElement('span');
          row.className = 'infobox-value-row';

          const baseName = path.split(/[\\/]/).pop() || path;
          const mono = document.createElement('span');
          mono.className = 'infobox-mono';
          mono.textContent = baseName;
          row.appendChild(mono);

          const icon = document.createElement('span');
          icon.className = 'info-icon';
          icon.textContent = '?';
          icon.setAttribute('aria-label', 'Full model path');
          attachDetailTooltip(icon, (box) => buildPathTooltipContent(box, path));
          row.appendChild(icon);

          col.appendChild(row);
        });
        addTechRow(modelPaths.length > 1 ? 'Models' : 'Model', col);
      }

      // Material Swaps — MSWP editor/form ID, plus (if AllRecords.csv
      // captured any .bgsm swap pairs for this record) a hoverable "?"
      // that reveals the raw source -> target swap list.
      if (item.MSWP_EditorID || item.MSWP_FormID) {
        const wrap = document.createElement('span');
        wrap.className = 'infobox-value-row';

        const mono = document.createElement('span');
        mono.className = 'infobox-mono';
        mono.textContent = `${item.MSWP_EditorID || 'Unknown'} (${item.MSWP_FormID || 'Unknown'})`;
        wrap.appendChild(mono);

        const materialPairs = (item.Materials || '').split(';').map(s => s.trim()).filter(Boolean);
        if (materialPairs.length) {
          const icon = document.createElement('span');
          icon.className = 'info-icon';
          icon.textContent = '?';
          icon.setAttribute('aria-label', 'Material swap details');
          attachDetailTooltip(icon, (box) => buildMaterialSwapTooltipContent(box, materialPairs), { sticky: true });
          wrap.appendChild(icon);
        }

        addTechRow('Material Swaps', wrap);
      }

      rows.appendChild(techAccordion);
      infobox.appendChild(rows);
      layout.appendChild(infobox);
      layout.appendChild(wrapper);

      // ── Unified right panel + storefront appended to wrapper below ──

      // ── Unified right panel ──
      const hasDesc = !!(item.ENTM_Description && item.ENTM_Description.trim());
      const hasCategory = (item.Category&&item.Category.trim()) || (item.SubCategory&&item.SubCategory.trim());
      const hasBook = !!(item.BOOK_FULL&&String(item.BOOK_FULL).trim());
      const hasEntm = !!(item.ENTM_FULL&&String(item.ENTM_FULL).trim());
      const hasWorkshopMax = item.Workshop_Max != null;
      const hasCAMPMax     = item.CAMP_Max != null;

      const panel = document.createElement('div');
      panel.className = 'right-panel';

      if (hasDesc) {
        const block = document.createElement('div');
        block.className = 'right-panel-block block-description';
        const h2 = document.createElement('h2'); h2.textContent = 'Description';
        block.appendChild(h2);
        const desc = document.createElement('div');
        desc.className = 'entm-description';
        desc.textContent = item.ENTM_Description.trim();
        block.appendChild(desc);
        panel.appendChild(block);
      }

      // ── Build Menu Category block ──
      if (hasCategory) {
        const block = document.createElement('div');
        block.className = 'right-panel-block block-category';
        const h2 = document.createElement('h2'); h2.textContent = 'Build Menu Category';
        block.appendChild(h2);

        // An item can be placeable from more than one build-menu location —
        // gather every distinct Category/SubCategory chain sharing this CNAM_FormID.
        const categoryChains = [];
        const seenChains = new Set();
        db.forEach(d => {
          if (d.CNAM_FormID !== item.CNAM_FormID) return;
          if (!(d.Category && d.Category.trim())) return;
          const chain = d.Category.toLowerCase() === 'wallpapers'
            ? ['Modify', d.Category]
            : (d.SubCategory && d.SubCategory.trim() ? [d.Category, d.SubCategory] : [d.Category]);
          const key = chain.join('>');
          if (seenChains.has(key)) return;
          seenChains.add(key);
          categoryChains.push(chain);
        });
        if (!categoryChains.length) {
          // Fallback: no other db rows matched (shouldn't normally happen since hasCategory is true)
          const chain = item.Category.toLowerCase() === 'wallpapers'
            ? ['Modify', item.Category]
            : (item.SubCategory && item.SubCategory.trim() ? [item.Category, item.SubCategory] : [item.Category]);
          categoryChains.push(chain);
        }

        categoryChains.forEach((chain, gi) => {
          const wrap = document.createElement('div'); wrap.className = 'detail-inline-wrap';
          chain.forEach((cat, i) => {
            wrap.appendChild(makePill(cat, CategoryIcons[cat]||'', ''));
            if (i < chain.length-1) wrap.appendChild(makeArrow());
          });
          block.appendChild(wrap);
          if (gi < categoryChains.length - 1) {
            const andRow = document.createElement('div');
            andRow.className = 'detail-connector-row';
            andRow.appendChild(makeConnector('and'));
            block.appendChild(andRow);
          }
        });

        panel.appendChild(block);
      }

      // ── Placement Conditions block ──
      if (hasBook || hasEntm) {
        const block = document.createElement('div');
        block.className = 'right-panel-block block-conditions';
        const h2 = document.createElement('h2'); h2.textContent = 'Placement Conditions';
        block.appendChild(h2);
        const unlockText = cnamUnlockText[String(item.CNAM_FormID||'').trim()];
        if (unlockText) { const note=document.createElement('div'); note.className='placement-note'; note.textContent=unlockText; block.appendChild(note); }
        const wrap = document.createElement('div'); wrap.className='detail-inline-wrap detail-inline-wrap--conditions';

        const bookSource = String(item.BOOK_SOURCE||'').trim();
        let bookBookConnector='and', bookEntmConnector='and', entmEntmConnector='and';
        if(bookSource==='CNDF'){bookBookConnector='or';bookEntmConnector='or';}
        else if(bookSource==='CNDFException'){bookBookConnector='or';bookEntmConnector='and';entmEntmConnector='or';}

        if (hasBook) {
          const bookFulls = String(item.BOOK_FULL||'').split(';').map(s=>s.trim()).filter(Boolean);
          let tagEntries;
          if(item.BOOK_SOURCE_TAG===null) tagEntries=[null];
          else if(Array.isArray(item.BOOK_SOURCE_TAG)) tagEntries=item.BOOK_SOURCE_TAG;
          else tagEntries=String(item.BOOK_SOURCE_TAG).split(/[,;]/).map(s=>s.trim()).filter(Boolean);

          const getTagsForBook = (idx) => {
            if(bookFulls.length===1) return tagEntries.map(t=>t);
            if(Array.isArray(tagEntries)&&tagEntries.length===bookFulls.length){const e=tagEntries[idx];if(e===null)return[null];return String(e).split(/[,;]/).map(t=>t.trim()).filter(Boolean);}
            return tagEntries.map(t=>t);
          };

          const bookPills = [];
          bookFulls.forEach((fullName, i) => {
            const tags = getTagsForBook(i);
            const uniqueNorm = [...new Set(tags.map(t=>t===null?'None':normalizeTag(t)))];
            const bookEditorId = (item.BOOK_EditorID||'').split(';')[i]?.trim()||'';
            const iconSrcs = uniqueNorm.map(nt=>getBookIconSrc(nt,bookEditorId,item.BOOK_SOURCE)).filter(Boolean);
            const flavorLines = uniqueNorm.map(nt=>getBookTooltip(nt,tags,bookEditorId,item.BOOK_SOURCE)).filter(Boolean);
            let pillText=fullName;
            if(/Challenge_/.test(bookEditorId)){pillText=`Challenge: "${fullName}"`;if(item.GLOB_Value!=null)pillText+=` (X/${item.GLOB_Value})`;}
            else if(String(item.BOOK_SOURCE||'').toLowerCase()==='learnbypickup') pillText=`Loot: ${fullName}`;
            bookPills.push(makePill(pillText, iconSrcs, flavorLines));
          });
          appendPillGroup(wrap, bookPills, bookBookConnector);
        }

        if (hasBook&&hasEntm) wrap.appendChild(makeConnector(bookEntmConnector));

        if (hasEntm) {
          const entmFulls = String(item.ENTM_FULL||'').split(';').map(s=>s.replace(/[\s;]*UTILITY_ENTM_ItemDisableEntitlement\s*\[ENTM:[0-9A-Fa-f]+\][\s;]*/gi,'').trim()).filter(Boolean);
          let entmEdids = String(item.ENTM_EDID||'').split(';').map(s=>s.trim()).filter(Boolean);
          const ExactPairs={'SCRAPBOX':'F1_ENTM_CAMP_ScrapBox_Standard','AMMO STORAGE BOX':'F1_ENTM_CAMP_AmmoStorageBox_Standard','AID BOX':'F1_ENTM_CAMP_AidBox_Standard'};
          const normStr=txt=>String(txt||'').toUpperCase().replace(/[^A-Z0-9]/g,'');
          function findRemove(pred){const idx=entmEdids.findIndex(pred);return idx>=0?entmEdids.splice(idx,1)[0]:'';}
          const reservedEdidMap={};
          entmFulls.forEach(full=>{const key=full.trim().toUpperCase();if(ExactPairs[key]){const exp=ExactPairs[key];const m=findRemove(e=>e.toUpperCase()===exp.toUpperCase())||findRemove(e=>e.toUpperCase().includes(exp.toUpperCase()))||findRemove(e=>exp.toUpperCase().includes(e.toUpperCase()));if(m)reservedEdidMap[full]=m;}});
          function pickBestEdid(fullName){if(!entmEdids.length)return'';const fn=normStr(fullName);let idx=entmEdids.findIndex(e=>normStr(e)===fn);if(idx>=0)return entmEdids.splice(idx,1)[0];idx=entmEdids.findIndex(e=>fullName.includes(e)||e.includes(fullName));if(idx>=0)return entmEdids.splice(idx,1)[0];const ft=fullName.toUpperCase().split(/[^A-Z0-9]+/).filter(Boolean);let bi=-1,bs=0;entmEdids.forEach((e,i)=>{const et=e.toUpperCase().split(/[^A-Z0-9]+/).filter(Boolean);const s=ft.reduce((s,t)=>s+(et.includes(t)?1:0),0);if(s>bs){bs=s;bi=i;}});if(bi>=0&&bs>0)return entmEdids.splice(bi,1)[0];return entmEdids.length?entmEdids.splice(0,1)[0]:'';}

          const entmPills=[];
          entmFulls.forEach(fullName=>{
            const matchedEdid=reservedEdidMap[fullName]||pickBestEdid(fullName);
            const edidUpper=String(matchedEdid||'').toUpperCase();
            const fullUpper=fullName.trim().toUpperCase();
            let iconSrc='';
            if(ExactPairs[fullUpper]&&edidUpper.startsWith('F1_ENTM'))iconSrc='../assets/SubCategory-AdditionalStorageIcon.svg';
            if(!iconSrc){for(const k in ManualEntmIcons){if(edidUpper.startsWith(k.toUpperCase())){iconSrc=ManualEntmIcons[k];break;}}}
            if(!iconSrc&&edidUpper.startsWith('F1_ENTM'))iconSrc='../assets/SubCategory-AdditionalStorageIcon.svg';
            if(!iconSrc){if(edidUpper.includes('ATX'))iconSrc='../assets/AtomIcon.webp';else if(/SCORE[_-]?MINISEASON/i.test(matchedEdid))iconSrc='../assets/SeasonIcon.webp';else if(/SCORE[_-]?S?\d+/i.test(matchedEdid)||edidUpper.includes('SCORE'))iconSrc='../assets/SeasonIcon.webp';}
            let tooltipText=fullName;
            for(const k in IconAltText_ENTM){if(edidUpper.startsWith(k.toUpperCase())){tooltipText=IconAltText_ENTM[k];break;}}
            if(tooltipText===fullName){for(const k in IconAltText_ENTM){if(edidUpper.includes(k.toUpperCase())){tooltipText=IconAltText_ENTM[k];break;}}}
            let textSuffix='';
            if(matchedEdid){const ak=Object.keys(miniSeasonAliases).find(k=>matchedEdid.startsWith(k));if(ak)textSuffix=` (Mini Season: ${miniSeasonAliases[ak]})`;else{const mm=matchedEdid.match(/MiniSeason_(?:\d+_)?([A-Za-z0-9]+)/i);if(mm)textSuffix=` (Mini Season: ${mm[1].replace(/([a-z])([A-Z])/g,'$1 $2')})`;else{const sm=matchedEdid.match(/SCORE[_]?S?(\d+)/i);if(sm)textSuffix=` (Season ${sm[1]})`;}}};
            entmPills.push(makePill(fullName+textSuffix, iconSrc ? [iconSrc] : [], tooltipText ? [tooltipText] : []));
          });
          appendPillGroup(wrap, entmPills, entmEntmConnector);
        }

        block.appendChild(wrap);
        finalizeConditionWrap(wrap);
        panel.appendChild(block);
      }

      // ── Player Buffs block ──
      const buffEntry = playerBuffInfo[String(item.CNAM_FormID || '').trim()];
      if (buffEntry && buffEntry.Buffs && buffEntry.Buffs.length) {
        const block = document.createElement('div');
        block.className = 'right-panel-block block-buffs';
        const h2 = document.createElement('h2'); h2.textContent = 'Player Buffs';
        block.appendChild(h2);
        renderPlayerBuffs(block, buffEntry.Buffs);
        panel.appendChild(block);
      }

      wrapper.appendChild(panel);

      // ── Bottom full-width row: Max Build Count + Storefront Images ──
      const hasStorefront  = !!(item.ENTM_ImagePath && item.ENTM_ImageNames && item.ENTM_ImageNames.trim());

      if (true) { // always show bottom row
        const bottomRow = document.createElement('div');
        bottomRow.className = 'storefront-row';

        // ── Assignables Info (shown first in this section, when present) ──
        // Support both the new { AllowedFormLists, ExcludedFormLists, Entries }
        // format and a legacy plain-array format.
        const _aiEntries = assignableInfo?.Entries || assignableInfo;
        const _aiFind = Array.isArray(_aiEntries) ? _aiEntries : [];
        const assignableEntry = _aiFind.find(a => a.CNAM_FormID === item.CNAM_FormID) || null;
        if (assignableEntry) {
          // Resolve string keys (new format) into actual item arrays so
          // the rest of the rendering code can treat AllowedItems /
          // ExcludedItems as plain arrays as before.
          if (assignableInfo?.AllowedFormLists || assignableInfo?.ExcludedFormLists) {
            const aKey = assignableEntry.AllowedItems;
            if (typeof aKey === 'string') {
              assignableEntry.AllowedItems = assignableInfo.AllowedFormLists[aKey] || [];
            }
            const eKey = assignableEntry.ExcludedItems;
            if (typeof eKey === 'string') {
              assignableEntry.ExcludedItems = assignableInfo.ExcludedFormLists[eKey] || [];
            }
          }
          const h2 = document.createElement('h2');
          h2.textContent = 'Assignables';
          h2.classList.add('h2--with-icon');
          const h2Icon = document.createElement('span');
          h2Icon.className = 'info-icon info-icon--h2';
          h2Icon.textContent = '?';
          h2Icon.setAttribute('aria-label', 'Assignables info');
          attachDetailTooltip(h2Icon, (box) => {
            box.classList.add('detail-tooltip--assignable');
            const line = document.createElement('div');
            line.className = 'detail-tooltip-line detail-tooltip-line--word-wrap';
            line.textContent = '"Inclusions" has the excluded items filtered out already. If something is listed under inclusions, that means it can be assigned. No need to double check the exclusions as well.';
            box.appendChild(line);
          });
          h2.appendChild(h2Icon);
          bottomRow.appendChild(h2);

          const infoLabel = document.createElement('div');
          infoLabel.className = 'assignable-label';
          const totalAssignable = assignableEntry.MaxAmounts ? assignableEntry.MaxAmounts.reduce((a, b) => a + b, 0) : assignableEntry.AssignableSlots;
          if (totalAssignable === -1) {
            infoLabel.textContent = `Allows infinite items to be assigned from the list "${assignableEntry.AllowedItemsList.Name}"`;
          } else if (totalAssignable == null) {
            infoLabel.textContent = `Allows items to be assigned from the list "${assignableEntry.AllowedItemsList.Name}"`;
          } else {
            infoLabel.textContent = `Allows ${totalAssignable} ${totalAssignable === 1 ? 'item' : 'items'} to be assigned from the list "${assignableEntry.AllowedItemsList.Name}"`;
          }
          bottomRow.appendChild(infoLabel);

          const excludedFormIDs = new Set((assignableEntry.ExcludedItems || []).map(x => x.FormID));
          const effectiveAllowedItems = assignableEntry.AllowedItems.filter(x => !excludedFormIDs.has(x.FormID));
          const sortedAllowedItems = [...effectiveAllowedItems].sort((a, b) => (a.Name || '').localeCompare(b.Name || ''));

          const grid = document.createElement('div');
          grid.className = 'assignable-grid';
          bottomRow.appendChild(grid);

          // `insideViewMore` = true for tiles rendered inside the "view more"
          // modal grid. Clicking one of those closes the view-more modal and
          // opens the item modal with a back arrow that re-opens it.
          function makeAssignableTile(displayItem, insideViewMore) {
            const a = document.createElement('a');
            a.className = 'assignable-item';
            a.href = '#';
            a.style.cursor = 'pointer';
            a.onclick = e => {
              e.preventDefault();
              if (insideViewMore) {
                _returningFromItem = true;
                const modalEl = a.closest('.assignable-modal');
                if (modalEl) assignableState.scrollPos = modalEl.scrollTop;
                if (_inclGridCtrl) {
                  assignableState.activePage = assignableState.activeTab === 1 && _exclGridCtrl
                    ? _exclGridCtrl.getPage()
                    : _inclGridCtrl.getPage();
                }
                closeViewMoreModal();
                openAssignableItemModal(displayItem, openViewMoreModal);
              } else {
                openAssignableItemModal(displayItem);
              }
            };
            const tileImg = document.createElement('img');
            tileImg.loading = 'lazy';
            if (displayItem.StaticRender) {
              tileImg.src = `../ModelRender/${displayItem.StaticRender}`;
            } else {
              const formID = (displayItem.FormID || '');
              const itemType = ((displayItem.ItemType || '').trim() || '_Other').replace(/\//g, '-');
              tileImg.src = `../ModelRender/Assignables/${itemType}/${formID.toUpperCase()}_thumbnail.webp`;
            }
            tileImg.alt = displayItem.Name || '';
            tileImg.onerror = function() {
              if (!tileImg._triedLower) {
                tileImg._triedLower = true;
                const formID = (displayItem.FormID || '');
                const itemType = ((displayItem.ItemType || '').trim() || '_Other').replace(/\//g, '-');
                tileImg.src = `../ModelRender/Assignables/${itemType}/${formID.toLowerCase()}_thumbnail.webp`;
              } else {
                this.onerror = null;
                this.src = '../assets/ImageNotFound.png';
              }
            };
            fadeImg(tileImg);
            a.appendChild(tileImg);
            const name = document.createElement('div');
            name.className = 'assignable-item-name';
            name.textContent = displayItem.Name;
            name.title = displayItem.Name;
            a.appendChild(name);
            return a;
          }

          // Tracks the currently-open "view more" modal's close function so
          // it can be closed cleanly when a tile inside it is clicked, or
          // reopened again via the item modal's back arrow.
          let viewMoreClose = null;
          function closeViewMoreModal() {
            if (viewMoreClose) { viewMoreClose(); viewMoreClose = null; }
          }

          const assignableState = { activeTab: 0, inclusionsSearch: '', exclusionsSearch: '', scrollPos: 0, activePage: 0, activeTypes: new Set() };
          let _returningFromItem = false;
          let _inclGridCtrl = null;
          let _exclGridCtrl = null;

          function openViewMoreModal() {
            if (!_returningFromItem) {
              assignableState.activeTab = 0;
              assignableState.inclusionsSearch = '';
              assignableState.exclusionsSearch = '';
              assignableState.scrollPos = 0;
              assignableState.activePage = 0;
              assignableState.activeTypes = new Set();
            }
            const wasReturning = _returningFromItem;
            _returningFromItem = false;

            const { modal, close, syncOverflow } = openModalShell({
                title: 'Assignables',
                onClose: () => {
                  if (!_returningFromItem) {
                    assignableState.activeTab = 0;
                    assignableState.inclusionsSearch = '';
                    assignableState.exclusionsSearch = '';
                    assignableState.scrollPos = 0;
                    assignableState.activePage = 0;
                    assignableState.activeTypes = new Set();
                  }
                }
              });
              viewMoreClose = close;

              // ── Collect unique ItemTypes per tab ──
              const TYPE_ORDER = ['Weapons','Armor','Apparel','Food/Drink','Aid','MISC','Holo','Notes','JUNK','MODS','Ammo'];
              const TYPE_DISPLAY = { Weapons:'Weapons', Armor:'Armor', Apparel:'Apparel', 'Food/Drink':'Food/Drink', Aid:'Aid', MISC:'Misc', Holo:'Holo', Notes:'Notes', JUNK:'Junk', MODS:'Mods', Ammo:'Ammo' };

              function typesForList(items) {
                const raw = [...new Set(items.map(x => (x.ItemType || '').trim()).filter(Boolean))];
                return raw.sort((a, b) => {
                  const ai = TYPE_ORDER.indexOf(a); const bi = TYPE_ORDER.indexOf(b);
                  return (ai === -1 ? 999 : ai) - (bi === -1 ? 999 : bi);
                });
              }
              const inclusionTypes = typesForList(effectiveAllowedItems);
              const exclusionTypes = typesForList(assignableEntry.ExcludedItems || []);
              const hasMultipleTypes = inclusionTypes.length + exclusionTypes.length > 1;

              // ── Header: tabs + search ──
              const stickyHeader = document.createElement('div');
              stickyHeader.className = 'modal-sticky-header';

              const tabsContainer = document.createElement('div');
              tabsContainer.className = 'assignable-modal-tabs';

              const tab1 = document.createElement('button');
              tab1.className = 'assignable-modal-tab active';
              tab1.textContent = 'Inclusions';

              const hasExclusions = assignableEntry.ExcludedItems && assignableEntry.ExcludedItems.length > 0;
              let tab2 = null;

              tabsContainer.appendChild(tab1);

              if (hasExclusions) {
                tab2 = document.createElement('button');
                tab2.className = 'assignable-modal-tab';
                tab2.textContent = 'Exclusions';
                tabsContainer.appendChild(tab2);
              }

              stickyHeader.appendChild(tabsContainer);

              // ── Search rows (one per tab) ──
              const searchRow1 = document.createElement('div');
              searchRow1.className = 'modal-search-row';
              const searchInput = document.createElement('input');
              searchInput.type = 'text';
              searchInput.placeholder = 'Search...';
              searchInput.className = 'modal-search-input';
              searchInput.addEventListener('focus', () => searchInput.style.borderColor = 'var(--accent)');
              searchInput.addEventListener('blur',  () => searchInput.style.borderColor = '#444');
              searchInput.addEventListener('input', () => { assignableState.inclusionsSearch = searchInput.value; });
              const pagerEl = document.createElement('div');
              pagerEl.className = 'modal-pager';
              searchRow1.appendChild(searchInput);
              searchRow1.appendChild(pagerEl);
              stickyHeader.appendChild(searchRow1);

              let searchRow2 = null;
              let excludedSearchInput = null;
              let excludedPagerEl = null;
              if (hasExclusions) {
                searchRow2 = document.createElement('div');
                searchRow2.className = 'modal-search-row';
                searchRow2.style.display = 'none';
                excludedSearchInput = document.createElement('input');
                excludedSearchInput.type = 'text';
                excludedSearchInput.placeholder = 'Search...';
                excludedSearchInput.className = 'modal-search-input';
                excludedSearchInput.addEventListener('focus', () => excludedSearchInput.style.borderColor = 'var(--accent)');
                excludedSearchInput.addEventListener('blur',  () => excludedSearchInput.style.borderColor = '#444');
                excludedSearchInput.addEventListener('input', () => { assignableState.exclusionsSearch = excludedSearchInput.value; });
                excludedPagerEl = document.createElement('div');
                excludedPagerEl.className = 'modal-pager';
                searchRow2.appendChild(excludedSearchInput);
                searchRow2.appendChild(excludedPagerEl);
                stickyHeader.appendChild(searchRow2);
              }

              // ── ItemType filter chips (rebuilt per tab) ──
              let filterRow = null;
              let filterChips = [];
              if (hasMultipleTypes) {
                filterRow = document.createElement('div');
                filterRow.className = 'modal-filter-row';
                stickyHeader.appendChild(filterRow);

                function rebuildFilterChips(tabIndex) {
                  const types = tabIndex === 1 ? exclusionTypes : inclusionTypes;
                  filterRow.replaceChildren();
                  filterChips = [];
                  types.forEach(type => {
                    const chip = document.createElement('button');
                    chip.type = 'button';
                    const isActive = assignableState.activeTypes.has(type);
                    chip.className = 'modal-filter-chip' + (isActive ? ' active' : '');
                    chip.textContent = TYPE_DISPLAY[type] || type;
                    filterRow.appendChild(chip);
                    filterChips.push({ chip, type });
                  });
                  filterRow.style.display = types.length > 1 ? '' : 'none';
                }
                rebuildFilterChips(0);
              }

              modal.appendChild(stickyHeader);

              function getActiveTypeFilter() {
                if (!hasMultipleTypes || assignableState.activeTypes.size === 0) return null;
                return item => assignableState.activeTypes.has((item.ItemType || '').trim());
              }

              // ── Tab content (grids only, no search rows inside) ──
              const tabContentWrapper = document.createElement('div');
              tabContentWrapper.className = 'assignable-modal-content-wrapper';
              // Attached to `modal` right away — before any grid content
              // exists inside it — so that when syncTabHeights measures
              // modal.getBoundingClientRect() during the grids' initial
              // render below, that content is already part of the live
              // layout tree and actually counted. Attaching it later (after
              // populating the grids) would mean the very first measurement
              // sees an incomplete tree and locks in a too-small height.
              modal.appendChild(tabContentWrapper);

              const tab1Content = document.createElement('div');
              tab1Content.className = 'assignable-modal-tab-content active';
              tabContentWrapper.appendChild(tab1Content);

              const includedLabel = document.createElement('div');
              includedLabel.style.cssText = 'font-size:12px;color:var(--text-muted);margin:12px 0;text-align:left;';
              includedLabel.textContent = `Allows the following items to be assigned from list "${assignableEntry.AllowedItemsList?.Name || 'Unknown'}"`;
              tab1Content.appendChild(includedLabel);

              const allGrid = document.createElement('div');
              allGrid.className = 'assignable-grid';
              tab1Content.appendChild(allGrid);
              const sortedAllowedForModal = [...effectiveAllowedItems].sort((a, b) => (a.Name || '').localeCompare(b.Name || ''));
              // Locks the MODAL'S OWN outer box height — not an inner
              // wrapper's min-height — to each tab's first, unfiltered
              // render, taking the taller of the two tabs once both have
              // been visited so switching tabs doesn't jump.
              //
              // This used to pad an inner wrapper instead, but that had a
              // real bug: if a tab's full unfiltered list was itself taller
              // than the modal's max-height (i.e. it already needed to
              // scroll to see everything), locking the wrapper to that full
              // height meant a later search — narrowing the list down to a
              // couple of items — still left the wrapper claiming all that
              // vertical space, so the modal stayed scrollable through what
              // was now mostly blank filler below the few visible tiles.
              // Locking the outer modal box instead is safe because the
              // browser has already clamped it to max-height by the time
              // it's measured, so the locked value can never exceed what's
              // actually visible on screen — a narrowed search just leaves
              // quiet, non-scrolling empty space within that same box.
              //
              // The read is synchronous (not requestAnimationFrame) and
              // gated on the tab's content actually being visible, because
              // this can be called during construction — before any saved
              // search has been restored, and before a returning-to-tab-2
              // visit has toggled tab2 visible — and a deferred read would
              // only ever see the LAST DOM state from this tick (already
              // narrowed by that restoration), not the natural one.
              let _tab1Height = 0;
              let _tab2Height = 0;
              function syncTabHeights() {
                const isTab2 = assignableState.activeTab === 1;
                const activeContent = isTab2 ? tab2Content : tab1Content;
                const visible = !!activeContent && activeContent.classList.contains('active');
                const alreadyLocked = isTab2 ? _tab2Height > 0 : _tab1Height > 0;
                if (visible && !alreadyLocked) {
                  const h = modal.getBoundingClientRect().height;
                  if (h > 0) {
                    if (isTab2) _tab2Height = h; else _tab1Height = h;
                    modal.style.height = Math.max(_tab1Height, _tab2Height) + 'px';
                  }
                }
                syncOverflow();
              }

              const inclGridCtrl = renderPaginatedGrid({
                items: sortedAllowedForModal,
                grid: allGrid,
                pagerEl,
                searchInput,
                tileFactory: displayItem => makeAssignableTile(displayItem, true),
                filterFn: getActiveTypeFilter(),
                onChange: syncTabHeights
              });
              _inclGridCtrl = inclGridCtrl;

              let tab2Content = null;
              let exclGridCtrl = null;
              if (hasExclusions) {
                tab2Content = document.createElement('div');
                tab2Content.className = 'assignable-modal-tab-content';
                // Same reasoning as tab1Content above: attach before
                // populating, so the initial (unfiltered) render this tab
                // gets the first time it's ever visible is measured
                // against a complete tree.
                tabContentWrapper.appendChild(tab2Content);

                const excludedLabel = document.createElement('div');
                excludedLabel.style.cssText = 'font-size:12px;color:var(--text-muted);margin:12px 0;text-align:left;';
                excludedLabel.textContent = `Explicitly excludes the following items from list "${assignableEntry.ExcludedItemsList?.Name || 'Unknown'}"`;
                tab2Content.appendChild(excludedLabel);

                const excludedGrid = document.createElement('div');
                excludedGrid.className = 'assignable-grid';
                tab2Content.appendChild(excludedGrid);
                const sortedExcludedItems = [...assignableEntry.ExcludedItems].sort((a, b) => (a.Name || '').localeCompare(b.Name || ''));
                exclGridCtrl = renderPaginatedGrid({
                  items: sortedExcludedItems,
                  grid: excludedGrid,
                  pagerEl: excludedPagerEl,
                  searchInput: excludedSearchInput,
                  tileFactory: displayItem => makeAssignableTile(displayItem, true),
                  filterFn: getActiveTypeFilter(),
                  onChange: syncTabHeights
                });
                _exclGridCtrl = exclGridCtrl;

                tab1.addEventListener('click', () => {
                  tab1.classList.add('active');
                  tab2.classList.remove('active');
                  tab1Content.classList.add('active');
                  tab2Content.classList.remove('active');
                  searchRow1.style.display = '';
                  searchRow2.style.display = 'none';
                  assignableState.activeTab = 0;
                  assignableState.activeTypes.clear();
                  if (filterRow) rebuildFilterChips(0);
                  inclGridCtrl.setFilter(null);
                  if (exclGridCtrl) exclGridCtrl.setFilter(null);
                });
                tab2.addEventListener('click', () => {
                  tab1.classList.remove('active');
                  tab2.classList.add('active');
                  tab1Content.classList.remove('active');
                  tab2Content.classList.add('active');
                  searchRow1.style.display = 'none';
                  searchRow2.style.display = '';
                  assignableState.activeTab = 1;
                  assignableState.activeTypes.clear();
                  if (filterRow) rebuildFilterChips(1);
                  inclGridCtrl.setFilter(null);
                  if (exclGridCtrl) exclGridCtrl.setFilter(null);
                });
              }

              // ── Filter chip click handlers ──
              if (filterRow) {
                filterRow.addEventListener('click', (e) => {
                  const chip = e.target.closest('.modal-filter-chip');
                  if (!chip) return;
                  const rawType = filterChips.find(c => c.chip === chip)?.type;
                  if (!rawType) return;
                  if (assignableState.activeTypes.has(rawType)) {
                    assignableState.activeTypes.delete(rawType);
                  } else {
                    assignableState.activeTypes.add(rawType);
                  }
                  const filterFn = assignableState.activeTypes.size === 0 ? null : getActiveTypeFilter();
                  filterChips.forEach(c => {
                    c.chip.classList.toggle('active', assignableState.activeTypes.has(c.type));
                  });
                  inclGridCtrl.setFilter(filterFn);
                  if (exclGridCtrl) exclGridCtrl.setFilter(filterFn);
                });
              }

              // Switch to the saved active tab
              if (hasExclusions && assignableState.activeTab === 1) {
                tab1.classList.remove('active');
                tab2.classList.add('active');
                tab1Content.classList.remove('active');
                tab2Content.classList.add('active');
                searchRow1.style.display = 'none';
                searchRow2.style.display = '';
                if (filterRow) rebuildFilterChips(1);
                // Capture tab2's natural height now, while its content is
                // still the full unfiltered list — the search restore right
                // below this may narrow it, and by then it'd be too late.
                syncTabHeights();
              }

              // Restore saved search state
              searchInput.value = assignableState.inclusionsSearch;
              searchInput.dispatchEvent(new Event('input'));
              if (hasExclusions && excludedSearchInput) {
                excludedSearchInput.value = assignableState.exclusionsSearch;
                excludedSearchInput.dispatchEvent(new Event('input'));
              }

              if (window.matchMedia('(hover: hover)').matches) searchInput.focus();

              // Restore page + scroll position when returning from item modal
              if (wasReturning && (assignableState.activePage > 0 || assignableState.scrollPos > 0)) {
                requestAnimationFrame(() => {
                  if (assignableState.activePage > 0) {
                    if (assignableState.activeTab === 1 && _exclGridCtrl) {
                      _exclGridCtrl.setPage(assignableState.activePage);
                    } else {
                      inclGridCtrl.setPage(assignableState.activePage);
                    }
                  }
                  if (assignableState.scrollPos > 0) modal.scrollTop = assignableState.scrollPos;
                });
              }

              // Height is already locked by this point (captured
              // synchronously at construction and, if applicable, right
              // after restoring tab2 as active — both before any saved
              // search could narrow the content). This call just refreshes
              // the scrollbar state for the final restored view.
              syncTabHeights();
            }

          sortedAllowedItems.forEach(displayItem => grid.appendChild(makeAssignableTile(displayItem)));
          if (sortedAllowedItems.length > 0) {
            registerPreviewGrid(grid, sortedAllowedItems.length, 'assignable-view-more', () => openViewMoreModal());
          }

          const sepAfterAssignable = document.createElement('hr');
          sepAfterAssignable.style.cssText = 'border:none;border-top:1px solid #383838;margin:16px 0;';
          bottomRow.appendChild(sepAfterAssignable);
        }

        if (true) { // always show max build count section
          const h2 = document.createElement('h2');
          h2.textContent = 'Max Build Count';
          bottomRow.appendChild(h2);

          const statsRow = document.createElement('div');
          statsRow.className = 'max-build-row';

          // ── CAMP first ──
          const campStat = document.createElement('div');
          campStat.className = 'max-build-stat';
          const campIcon = document.createElement('img');
          campIcon.src = '../assets/CAMPIcon.svg'; campIcon.alt = 'C.A.M.P.';
          const campWrap = document.createElement('div');
          campWrap.style.cssText = 'display:flex;flex-direction:column;gap:2px;';
          const campLbl = document.createElement('div'); campLbl.className = 'max-build-stat-label'; campLbl.textContent = 'C.A.M.P.';
          const campVal = document.createElement('div'); campVal.className = 'max-build-stat-value';
          campVal.textContent = hasCAMPMax ? item.CAMP_Max : 'None';
          if (!hasCAMPMax) campVal.style.color = '#666';
          campWrap.appendChild(campLbl); campWrap.appendChild(campVal);
          campStat.appendChild(campIcon); campStat.appendChild(campWrap);
          statsRow.appendChild(campStat);

          // ── Workshop second ──
          const wsStat = document.createElement('div');
          wsStat.className = 'max-build-stat';
          const wsIcon = document.createElement('img');
          wsIcon.src = '../assets/WorkshopIcon.webp'; wsIcon.alt = 'Workshop';
          const wsWrap = document.createElement('div');
          wsWrap.style.cssText = 'display:flex;flex-direction:column;gap:2px;';
          const wsLbl = document.createElement('div'); wsLbl.className = 'max-build-stat-label'; wsLbl.textContent = 'Workshop';
          const wsVal = document.createElement('div'); wsVal.className = 'max-build-stat-value';
          wsVal.textContent = hasWorkshopMax ? item.Workshop_Max : 'None';
          if (!hasWorkshopMax) wsVal.style.color = '#666';
          wsWrap.appendChild(wsLbl); wsWrap.appendChild(wsVal);
          wsStat.appendChild(wsIcon); wsStat.appendChild(wsWrap);
          statsRow.appendChild(wsStat);

          bottomRow.appendChild(statsRow);

          // ── Shares max build count with ──
          const globKey = item.Workshop_MaxFormID || item.CAMP_Max_FormID;
          const sharedLabel = document.createElement('div');
          sharedLabel.className = 'shared-label';
          sharedLabel.textContent = 'Shares Max Build Count With';
          bottomRow.appendChild(sharedLabel);

          if (globKey) {
            const shared = db.filter(d =>
              d.CNAM_FormID !== item.CNAM_FormID &&
              d.Name && d.Name !== 'null' &&
              (d.Workshop_MaxFormID === globKey || d.CAMP_Max_FormID === globKey)
            );
            const seen = new Set();
            const uniqueShared = shared.filter(d => {
              if (seen.has(d.CNAM_FormID)) return false;
              seen.add(d.CNAM_FormID); return true;
            }).sort((a, b) => (a.Name || '').localeCompare(b.Name || ''));

            if (uniqueShared.length > 0) {
              const grid = document.createElement('div');
              grid.className = 'shared-grid';
              bottomRow.appendChild(grid);

              function makeSharedTile(d) {
                const a = document.createElement('a');
                a.className = 'shared-item';
                a.href = buildItemViewUrl(d);
                a.onclick = e => {
                  e.preventDefault();
                  window.location.assign(a.href);
                };
                const img = document.createElement('img');
                img.loading = 'lazy';
                const sharedFormID = ((d.ARTO_FormID||d.CNAM_FormID||'')).toLowerCase();
                img.src = `../WorkshopIcons/${sharedFormID}.webp`;
                img.alt = d.Name;
                img.onerror = function() { this.onerror=null; this.src = `../WorkshopIcons/item-placeholder.webp`; };
                fadeImg(img);
                const name = document.createElement('div');
                name.className = 'shared-item-name';
                name.textContent = d.Name;
                name.title = d.Name;
                a.appendChild(img); a.appendChild(name);
                return a;
              }

              uniqueShared.forEach(d => grid.appendChild(makeSharedTile(d)));
              registerPreviewGrid(grid, uniqueShared.length, 'shared-view-more', () => {
                const { modal, syncOverflow } = openModalShell({ title: 'Shared Max Build Count' });

                // Search bar + pager, wrapped in the same sticky-header
                // shell the assignables modal uses so it stays pinned
                // while the grid below scrolls.
                const stickyHeader = document.createElement('div');
                stickyHeader.className = 'modal-sticky-header';
                const searchRow = document.createElement('div');
                searchRow.className = 'modal-search-row';
                const searchInput = document.createElement('input');
                searchInput.type = 'text';
                searchInput.placeholder = 'Search...';
                searchInput.className = 'modal-search-input';
                searchInput.addEventListener('focus', () => searchInput.style.borderColor = 'var(--accent)');
                searchInput.addEventListener('blur',  () => searchInput.style.borderColor = '#444');
                const pagerEl = document.createElement('div');
                pagerEl.className = 'modal-pager';
                searchRow.appendChild(searchInput);
                searchRow.appendChild(pagerEl);
                stickyHeader.appendChild(searchRow);
                modal.appendChild(stickyHeader);

                const allGrid = document.createElement('div');
                allGrid.className = 'shared-grid';
                modal.appendChild(allGrid);
                renderPaginatedGrid({
                  items: uniqueShared,
                  grid: allGrid,
                  pagerEl,
                  searchInput,
                  tileFactory: makeSharedTile,
                  onChange: createHeightLock(modal, syncOverflow)
                });

                if (window.matchMedia('(hover: hover)').matches) searchInput.focus();
              });
            } else {
              // No shared items found
              const none = document.createElement('div');
              none.className = 'shared-none';
              none.textContent = 'None';
              bottomRow.appendChild(none);
            }
          } else {
            // No glob key — no sharing data
            const none = document.createElement('div');
            none.className = 'shared-none';
            none.textContent = 'None';
            bottomRow.appendChild(none);
          }
        }

        // ── Shared Build Menu Slot ──
        if (item.COBJ_FormID) {
          const sep = document.createElement('hr');
          sep.style.cssText = 'border:none;border-top:1px solid #383838;margin:16px 0;';
          bottomRow.appendChild(sep);

          const h2 = document.createElement('h2');
          h2.textContent = 'Build Menu Slot';
          bottomRow.appendChild(h2);

          const slotLabel = document.createElement('div');
          slotLabel.className = 'shared-label';
          slotLabel.textContent = 'Shares a Build Menu Slot With';
          bottomRow.appendChild(slotLabel);

          const slotShared = db.filter(d =>
            d.CNAM_FormID !== item.CNAM_FormID &&
            d.Name && d.Name !== 'null' &&
            d.COBJ_FormID === item.COBJ_FormID
          );
          const slotSeen = new Set();
          const uniqueSlot = slotShared.filter(d => {
            if (slotSeen.has(d.CNAM_FormID)) return false;
            slotSeen.add(d.CNAM_FormID); return true;
          }).sort((a, b) => (a.Name || '').localeCompare(b.Name || ''));

          if (uniqueSlot.length > 0) {
            const grid = document.createElement('div');
            grid.className = 'shared-grid';
            bottomRow.appendChild(grid);

            function makeSlotTile(d) {
              const a = document.createElement('a');
              a.className = 'shared-item';
              a.href = buildItemViewUrl(d);
              a.onclick = e => {
                e.preventDefault();
                window.location.assign(a.href);
              };
              const img = document.createElement('img');
              img.loading = 'lazy';
              img.src = d.ARTO_FormID ? `../WorkshopIcons/${d.ARTO_FormID.toLowerCase()}.webp` : `../WorkshopIcons/${(d.CNAM_FormID||'').toLowerCase()}.webp`;
              img.alt = d.Name;
              img.onerror = function() { this.onerror=null; this.src=`../WorkshopIcons/${(d.CNAM_FormID||'').toLowerCase()}.webp`; };
              fadeImg(img);
              const name = document.createElement('div');
              name.className = 'shared-item-name';
              name.textContent = d.Name;
              name.title = d.Name;
              a.appendChild(img); a.appendChild(name);
              return a;
            }

            uniqueSlot.forEach(d => grid.appendChild(makeSlotTile(d)));
            registerPreviewGrid(grid, uniqueSlot.length, 'shared-view-more', () => {
              const { modal, syncOverflow } = openModalShell({ title: 'Build Menu Slot' });

              // Search bar + pager, wrapped in the same sticky-header
              // shell the assignables modal uses so it stays pinned
              // while the grid below scrolls.
              const stickyHeader = document.createElement('div');
              stickyHeader.className = 'modal-sticky-header';
              const searchRow = document.createElement('div');
              searchRow.className = 'modal-search-row';
              const searchInput = document.createElement('input');
              searchInput.type = 'text';
              searchInput.placeholder = 'Search...';
              searchInput.className = 'modal-search-input';
              searchInput.addEventListener('focus', () => searchInput.style.borderColor = 'var(--accent)');
              searchInput.addEventListener('blur',  () => searchInput.style.borderColor = '#444');
              const pagerEl = document.createElement('div');
              pagerEl.className = 'modal-pager';
              searchRow.appendChild(searchInput);
              searchRow.appendChild(pagerEl);
              stickyHeader.appendChild(searchRow);
              modal.appendChild(stickyHeader);

              const allGrid = document.createElement('div');
              allGrid.className = 'shared-grid';
              modal.appendChild(allGrid);
              renderPaginatedGrid({
                items: uniqueSlot,
                grid: allGrid,
                pagerEl,
                searchInput,
                tileFactory: makeSlotTile,
                onChange: createHeightLock(modal, syncOverflow)
              });

              if (window.matchMedia('(hover: hover)').matches) searchInput.focus();
            });
          } else {
            const none = document.createElement('div');
            none.className = 'shared-none';
            none.textContent = 'None';
            bottomRow.appendChild(none);
          }
        }

        // ── Storefront Images ──
        if (hasStorefront) {
          const sep = document.createElement('hr');
          sep.style.cssText = 'border:none;border-top:1px solid #383838;margin:16px 0;';
          bottomRow.appendChild(sep);

          const h2 = document.createElement('h2');
          h2.textContent = 'Storefront Images';
          bottomRow.appendChild(h2);

          const basePath = item.ENTM_ImagePath.trim().replace(/\\/g, '/');
          const imageNames = item.ENTM_ImageNames.split(';').map(s => s.trim()).filter(Boolean);
          const grid = document.createElement('div');
          grid.className = 'storefront-grid';

          let storefrontPending = imageNames.length;
          function checkStorefrontEmpty() {
            if (--storefrontPending === 0 && grid.children.length === 0) {
              sep.remove();
              h2.remove();
            }
          }

          imageNames.forEach(name => {
            const baseName = name.replace(/\.dds$/i, '');
            const relativePath = basePath.replace(/^Textures\//i, '');
            const lowerRelative = relativePath.toLowerCase();

            // Normalise the many on-disk spellings that arise from DB paths like
            // "TGATextures/ATX/Storefront/Camp/Ally/" → files live under either
            //   Storefront/atx/…      (clean path, most common)
            //   Storefront/tgaatx/…  (TGA-prefixed folder, also present on disk)
            // Strategy: derive the canonical "atx/…" form first, then the "tgaatx/…"
            // variant, and keep the original as a last-resort fallback.
            const lowerRelativeClean = lowerRelative
              .replace(/^tgatextures\/atx\//, 'atx/')   // tgatextures/atx/… → atx/…
              .replace(/^tgaatx\//, 'atx/');             // tgaatx/…          → atx/…

            const lowerRelativeTGAATX = lowerRelativeClean
              .replace(/^atx\//, 'tgaatx/');             // atx/…             → tgaatx/…

            // Collect unique path prefixes to try, in priority order
            const relativeVariants = [...new Set([lowerRelativeClean, lowerRelativeTGAATX, lowerRelative])];

            const lowerBase = baseName.toLowerCase();
            const srcNoATX = lowerBase.replace(/^atx_/, '');

            // Generate multiple attempts across all relative-path variants × filename patterns
            const attemptsList = relativeVariants.flatMap(rel => [
              `../Storefront/${rel}${lowerBase}.webp`,
              `../Storefront/${rel}${lowerBase}_l.webp`,
              `../Storefront/${rel}${srcNoATX}.webp`,
              `../Storefront/${rel}${srcNoATX}_l.webp`,
              // Try just doubling underscore before 's' (common in SirLoin pattern)
              `../Storefront/${rel}${srcNoATX.replace(/_s/, '__s')}.webp`,
              `../Storefront/${rel}${srcNoATX.replace(/_s/, '__s')}_l.webp`,
              // Try all underscores doubled
              `../Storefront/${rel}${srcNoATX.replace(/_/g, '__')}.webp`,
              `../Storefront/${rel}${srcNoATX.replace(/_/g, '__')}_l.webp`,
            ]);
            // Remove duplicates while preserving order
            const attempts = [...new Set(attemptsList)];

            const thumb = document.createElement('div');
            thumb.className = 'storefront-thumb';
            thumb.title = baseName;

            const img = document.createElement('img');
            img.src = attempts[0];
            img.alt = baseName;
            img.loading = 'lazy';
            let attemptIndex = 0;
            img.onerror = function() {
              attemptIndex++;
              if (attemptIndex < attempts.length) {
                this.src = attempts[attemptIndex];
              } else {
                this.onerror = null;
                thumb.remove();
                checkStorefrontEmpty();
              }
            };
            fadeImg(img);

            thumb.appendChild(img);
            thumb.addEventListener('click', () => openLightbox(img, () => {}));
            grid.appendChild(thumb);
          });

          bottomRow.appendChild(grid);
        }

        // ── Model Renders (always shown) ──
        const renderSep = document.createElement('hr');
        renderSep.style.cssText = 'border:none;border-top:1px solid #383838;margin:16px 0;';
        bottomRow.appendChild(renderSep);

        const renderHeading = document.createElement('h2');
        renderHeading.textContent = 'Model Renders';
        bottomRow.appendChild(renderHeading);

        const renderGrid = document.createElement('div');
        renderGrid.className = 'model-renders-grid';

        let renderPending = 3;
        function checkRendersEmpty() {
          if (--renderPending === 0 && renderGrid.children.length === 0) {
            renderSep.remove();
            renderHeading.remove();
          }
        }

        const renderSuffixes = ['_1', '_2', '_3'];
        renderSuffixes.forEach(suffix => {
          const renderThumb = document.createElement('div');
          renderThumb.className = 'storefront-thumb';
          const renderImg = document.createElement('img');
          const renderSrc = `../ModelRender/CAMPitem/${cnamFormID.toUpperCase()}${suffix}.webp`;
          renderImg.src = renderSrc;
          renderImg.alt = `Render ${suffix}`;
          renderImg.loading = 'lazy';
          renderImg.onerror = function() {
            if (!renderImg._triedLower) {
              renderImg._triedLower = true;
              renderImg.src = `../ModelRender/CAMPitem/${cnamFormID}${suffix}.webp`;
            } else {
              this.onerror = null;
              renderThumb.remove();
              checkRendersEmpty();
            }
          };
          fadeImg(renderImg);
          renderThumb.appendChild(renderImg);
          renderThumb.addEventListener('click', () => openLightbox(renderImg, () => {}));
          renderGrid.appendChild(renderThumb);
        });

        bottomRow.appendChild(renderGrid);

        wrapper.appendChild(bottomRow);
      }

      contentDiv.appendChild(layout);
      trimPreviewGrids();
      requestAnimationFrame(() => {
        void contentDiv.offsetWidth;
        requestAnimationFrame(() => {
          contentDiv.classList.remove('detail-content-loading');
          contentDiv.classList.add('detail-content-ready');
        });
      });
      document.title = item.Name;
    }
